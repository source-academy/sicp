    <SUBSECTION>
      <NAME>
  Implementing the Query System
      </NAME>

      <LABEL NAME="sec:implementing-the-query-system"/>
      <TEXT>
  section<SPACE/><REF NAME="sec:how-query-works"/> described how the query system
  works. Now we fill in the details by presenting a complete
  implementation of the system.
      </TEXT>

      <SUBSUBSECTION>
  <NAME>The Driver Loop and Instantiation</NAME>

  <LABEL NAME="sec:query-driver"/>
  <TEXT>
    <INDEX>driver loop<SUBINDEX>query@in query interpreter</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>driver loop|(</SUBINDEX></INDEX>
    The driver loop for the query system repeatedly reads input
    expressions.  If the expression is a rule or assertion to be added to
    the data base, then the information is added.  Otherwise the
    expression is assumed to be a query.  The driver passes this query to
    the evaluator <SCHEMEINLINE>qeval</SCHEMEINLINE> together with an initial frame stream
    consisting of a single empty frame.  The result of the evaluation is a
    stream of frames generated by satisfying the query with variable
    values found in the data base.  These frames are used to form a new
    stream consisting of copies of the original query in which the
    variables are instantiated with values supplied by the stream of
    frames, and this final stream is printed at the terminal:

    <SNIPPET>
      <SCHEME>
        <INDEX>prompts<SUBINDEX>query interpreter</SUBINDEX></INDEX>
        (define input-prompt ";;; Query input:")
        (define output-prompt ";;; Query results:")

        <!--  \indcode*{query-driver-loop} -->
        (define (query-driver-loop)
        (prompt-for-input input-prompt)
        (let ((q (query-syntax-process (read))))
        (cond ((assertion-to-be-added? q)
              (add-rule-or-assertion! (add-assertion-body q))
              (newline)
              (display "Assertion added to data base.")
              (query-driver-loop))
              (else
              (newline)
              (display output-prompt)
              (display-stream
              (stream-map
              (lambda (frame)
              (instantiate q
              frame
              (lambda (v f)
              (contract-question-mark v))))
              (qeval q (singleton-stream '()))))
              (query-driver-loop)))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <INDEX>abstract syntax<SUBINDEX>in query interpreter</SUBINDEX></INDEX>
    Here, as in the other evaluators in this chapter, we use an abstract
    syntax for the expressions of the query language.
    The implementation of the expression syntax, including the predicate
    <SCHEMEINLINE>assertion-to-be-added?</SCHEMEINLINE> and the selector <SCHEMEINLINE>add-assertion-body</SCHEMEINLINE>,
    is given in section<SPACE/><REF NAME="sec:query-syntax"/>.
    <SCHEMEINLINE>Add\?rule\?or-assertion!</SCHEMEINLINE> is defined in section<SPACE/><REF NAME="sec:query-db"/>.
  </TEXT>

  <TEXT>
    Before doing any processing on an input expression, the driver loop
    transforms it syntactically into a form that makes the processing more
    efficient.  This involves changing the 
    <INDEX>pattern variable<SUBINDEX>representation of</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>pattern-variable representation</SUBINDEX></INDEX>
    representation of pattern
    variables.  When the query is instantiated, any variables that remain
    unbound are transformed back to the input representation before being
    printed.  These transformations are performed by the two
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    <SCHEMEINLINE>query-syntax\?process</SCHEMEINLINE> and <SCHEMEINLINE>contract-question-mark</SCHEMEINLINE>
    (section <SPACE/><REF NAME="sec:query-syntax"/>).
  </TEXT>

  <TEXT>
    <INDEX>query interpreter<SUBINDEX>instantiation|(</SUBINDEX></INDEX>
    To instantiate an expression, we copy it, replacing any variables in
    the expression by their values in a given frame.  The values are
    themselves instantiated, since they could contain variables (for
    example, if <SCHEMEINLINE>?x</SCHEMEINLINE> in <SCHEMEINLINE>exp</SCHEMEINLINE> is bound to <SCHEMEINLINE>?y</SCHEMEINLINE> as the result
    of unification and <SCHEMEINLINE>?y</SCHEMEINLINE> is in turn bound to<SPACE/>5).  The action to
    take if a variable cannot be instantiated is given by a procedural
    argument to <SCHEMEINLINE>instantiate</SCHEMEINLINE>.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{instantiate} -->
        (define (instantiate exp frame unbound-var-handler)
        (define (copy exp)
        (cond ((var? exp)
              (let ((binding (binding-in-frame exp frame)))
              (if binding
              (copy (binding-value binding))
              (unbound-var-handler exp frame))))
              ((pair? exp)
              (cons (copy (car exp)) (copy (cdr exp))))
              (else exp)))
        (copy exp))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    that manipulate bindings are defined in
    section<SPACE/><REF NAME="sec:query-bindings"/>.
    <INDEX>query interpreter<SUBINDEX>instantiation|)</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>driver loop|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>The Evaluator</NAME>

  <LABEL NAME="sec:query-eval"/>
  <TEXT>
    <INDEX>query interpreter<SUBINDEX>query evaluator|(</SUBINDEX></INDEX>
    The <SCHEMEINLINE>qeval</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>, called by the <SCHEMEINLINE>query-driver-loop</SCHEMEINLINE>, is
    the basic evaluator of the query system.  It takes as inputs a query
    and a stream of frames, and it returns a stream of extended frames.
    It identifies special forms by a 
    <INDEX>data-directed programming<SUBINDEX>in query interpreter</SUBINDEX></INDEX>
    data-directed dispatch using <SCHEMEINLINE>get</SCHEMEINLINE> and <SCHEMEINLINE>put</SCHEMEINLINE>, just as we did in implementing generic operations
    in chapter<SPACE/>2.  Any query that is not identified as a special form is
    assumed to be a simple query, to be processed by <SCHEMEINLINE>simple-query</SCHEMEINLINE>.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{qeval} -->
        (define (qeval query frame-stream)
        (let ((qproc (get (type query) 'qeval)))
        (if qproc
              (qproc (contents query) frame-stream)
              (simple-query query frame-stream))))
      </SCHEME>
    </SNIPPET>

    <SCHEMEINLINE>Type</SCHEMEINLINE> and <SCHEMEINLINE>contents</SCHEMEINLINE>, defined in section<SPACE/><REF NAME="sec:query-syntax"/>,
    implement the abstract syntax of the special forms.
  </TEXT>

  <SUBHEADING>
    <NAME>Simple queries</NAME>
  </SUBHEADING>

  <INDEX>simple query<SUBINDEX>processing|(</SUBINDEX></INDEX>
  <TEXT>
    The <SCHEMEINLINE>simple-query</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    handles simple queries.  It takes as
    arguments a simple query (a pattern) together with a stream of frames,
    and it returns the stream formed by extending each frame by all
    data-base matches of the query.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{simple-query} -->
        (define (simple-query query-pattern frame-stream)
        (stream-flatmap
        (lambda (frame)
        (stream-append-delayed
        (find-assertions query-pattern frame)
        (delay (apply-rules query-pattern frame))))
        frame-stream))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    For each frame in the input stream, we use <SCHEMEINLINE>find-assertions</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-match"/>) to match the pattern against all
    assertions in the data base, producing a stream of extended frames,
    and we use <SCHEMEINLINE>apply-rules</SCHEMEINLINE> (section<SPACE/><REF NAME="sec:query-unify"/>) to apply
    all possible rules, producing another stream of extended frames.
    These two streams are combined (using <SCHEMEINLINE>stream\?append\?delayed</SCHEMEINLINE>,
    section<SPACE/><REF NAME="sec:query-streams"/>) to make a stream of all the ways that
    the given pattern can be satisfied consistent with the original frame
    (see exercise<SPACE/><REF NAME="ex:q-why-not-append"/>).  The streams for the
    individual input frames are combined using <SCHEMEINLINE>stream-flatmap</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-streams"/>) to form one large stream of all the
    ways that any of the frames in the original input stream can be
    extended to produce a match with the given pattern.
    <INDEX>simple query<SUBINDEX>processing|)</SUBINDEX></INDEX>
  </TEXT>

  <SUBHEADING>
    <NAME>Compound queries</NAME>
  </SUBHEADING>

  <INDEX>compound query<SUBINDEX>processing|(</SUBINDEX></INDEX>
  <TEXT>
    <!-- \indcodeplus{and}{(query language)}[evaluation of]-->
    <SCHEMEINLINE>And</SCHEMEINLINE> queries are handled as illustrated in
    Figure<SPACE/><REF NAME="fig:query-and"/> by the <SCHEMEINLINE>conjoin</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>.  <SCHEMEINLINE>Conjoin</SCHEMEINLINE> takes as inputs the conjuncts and the frame stream and
    returns the stream of extended frames.  First, <SCHEMEINLINE>conjoin</SCHEMEINLINE> processes
    the stream of frames to find the stream of all possible frame extensions
    that satisfy the first query in the conjunction.  Then, using this as the new
    frame stream, it recursively applies <SCHEMEINLINE>conjoin</SCHEMEINLINE> to the rest of the
    queries.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{conjoin} -->
        (define (conjoin conjuncts frame-stream)
        (if (empty-conjunction? conjuncts)
        frame-stream
        (conjoin (rest-conjuncts conjuncts)
              (qeval (first-conjunct conjuncts)
              frame-stream))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The expression
    <SNIPPET>
      <SCHEME>
        (put 'and 'qeval conjoin)
      </SCHEME>
    </SNIPPET>
    sets up <SCHEMEINLINE>qeval</SCHEMEINLINE> to dispatch to <SCHEMEINLINE>conjoin</SCHEMEINLINE> when an <SCHEMEINLINE>and</SCHEMEINLINE>
    form is encountered.
  </TEXT>

  <TEXT>
    <!--  \indcodeplus{or}{(query language)}[evaluation of]-->
    <SCHEMEINLINE>Or</SCHEMEINLINE> queries are handled similarly, as shown in
    Figure<SPACE/><REF NAME="fig:query-or"/>.  The output streams for the various
    disjuncts of the <SCHEMEINLINE>or</SCHEMEINLINE> are computed separately and merged using the
    <SCHEMEINLINE>interleave-delayed</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    from section<SPACE/><REF NAME="sec:query-streams"/>.
    (See exercises<SPACE/><REF NAME="ex:q-why-not-append"/> and<SPACE/><REF NAME="ex:q-why-interleave"/>.)

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{disjoin} -->
        (define (disjoin disjuncts frame-stream)
        (if (empty-disjunction? disjuncts)
        the-empty-stream
        (interleave-delayed
        (qeval (first-disjunct disjuncts) frame-stream)
        (delay (disjoin (rest-disjuncts disjuncts)
              frame-stream)))))

        (put 'or 'qeval disjoin)
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The predicates and selectors for the syntax of conjuncts and disjuncts
    are given in section<SPACE/><REF NAME="sec:query-syntax"/>.
  </TEXT>

  <SUBHEADING>
    <NAME>Filters</NAME>
  </SUBHEADING>

  <TEXT>
    <!-- \indcodeplus{not}{(query language)}[evaluation of]-->
    <SCHEMEINLINE>Not</SCHEMEINLINE> is handled by the method outlined in
    section<SPACE/><REF NAME="sec:how-query-works"/>.  We attempt to extend each frame in
    the input stream to satisfy the query being negated, and we include a
    given frame in the output stream only if it cannot be extended.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{negate} -->
        (define (negate operands frame-stream)
        (stream-flatmap
        (lambda (frame)
        (if (stream-null? (qeval (negated-query operands)
              (singleton-stream frame)))
              (singleton-stream frame)
              the-empty-stream))
        frame-stream))

        (put 'not 'qeval negate)
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <!-- \indcodeplus{lisp-value}{(query language)}[evaluation of]-->
    <SCHEMEINLINE><SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE>-value</SCHEMEINLINE> is a filter similar to <SCHEMEINLINE>not</SCHEMEINLINE>.  Each frame in the
    stream is used to instantiate the variables in the pattern, the
    indicated predicate is applied, and the frames for which the predicate
    returns false are filtered out of the input stream.  An error results
    if there are unbound pattern variables.

    <SNIPPET>
      <SCHEME>
        <!-- \indcodeplus*{lisp-value}{(query interpreter)}-->
        (define (lisp-value call frame-stream)
        (stream-flatmap
        (lambda (frame)
        (if (execute
              (instantiate
              call
              frame
              (lambda (v f)
              (error "Unknown pat var - - LISP-VALUE" v))))
              (singleton-stream frame)
              the-empty-stream))
        frame-stream))

        (put 'lisp-value 'qeval lisp-value)
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Execute</SCHEMEINLINE>, which applies the predicate to the arguments, must <SCHEMEINLINE>eval</SCHEMEINLINE> the predicate expression to get the
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    to apply.
    However, it must not evaluate the arguments, since they are already
    the actual arguments, not expressions whose evaluation (in <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE>) will
    produce the arguments.  Note that <SCHEMEINLINE>execute</SCHEMEINLINE> is implemented using
    <!-- \indprim{eval}[used in query interpreter]-->
    <SCHEMEINLINE>eval</SCHEMEINLINE> and <SCHEMEINLINE>apply</SCHEMEINLINE> from the underlying <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE> system.

    <SNIPPET>
      <SCHEME>
        (define (execute exp)
        (apply (eval (predicate exp) user-initial-environment)
              (args exp)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The <SCHEMEINLINE>always-true</SCHEMEINLINE> special form provides for a query that is always
    satisfied.  It ignores its contents (normally empty) and simply passes
    through all the frames in the input stream.  <SCHEMEINLINE>Always-true</SCHEMEINLINE> is used
    by the <SCHEMEINLINE>rule-body</SCHEMEINLINE> selector (section<SPACE/><REF NAME="sec:query-syntax"/>)
    <INDEX>rule (query language)<SUBINDEX>without body</SUBINDEX></INDEX>
    to provide bodies for rules that were
    defined without bodies (that is, rules whose conclusions are always
    satisfied).

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{always-true} -->
        (define (always-true ignore frame-stream) frame-stream)

        (put 'always-true 'qeval always-true)
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The selectors that define the syntax of <SCHEMEINLINE>not</SCHEMEINLINE> and <SCHEMEINLINE>lisp-value</SCHEMEINLINE>
    are given in section<SPACE/><REF NAME="sec:query-syntax"/>.
    <INDEX>compound query<SUBINDEX>processing|)</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>query evaluator|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Finding Assertions by Pattern Matching</NAME>

  <LABEL NAME="sec:query-match"/>
  <TEXT>
    <INDEX>query interpreter<SUBINDEX>pattern matching|(</SUBINDEX></INDEX>
    <INDEX>pattern matching<SUBINDEX>implementation|(</SUBINDEX></INDEX>
    <SCHEMEINLINE>Find-assertions</SCHEMEINLINE>, called by <SCHEMEINLINE>simple-query</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-eval"/>), takes as input a pattern and a frame.
    It returns a stream of frames, each extending the given one by a
    data-base match of the given pattern.  It uses <SCHEMEINLINE>fetch-assertions</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-db"/>) to get a stream of all the assertions in
    the data base that should be checked for a match against the pattern
    and the frame.  The reason for <SCHEMEINLINE>fetch-assertions</SCHEMEINLINE> here is that we
    can often apply simple tests that will eliminate many of the entries
    in the data base from the pool of candidates for a successful match.
    The system would still work if we eliminated <SCHEMEINLINE>fetch-assertions</SCHEMEINLINE>
    and simply checked a stream of all assertions in the data base, but
    the computation would be less efficient because we would need to make
    many more calls to the matcher.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{find-assertions} -->
        (define (find-assertions pattern frame)
        (stream-flatmap (lambda (datum)
              (check-an-assertion datum pattern frame))
              (fetch-assertions pattern frame)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Check-an-assertion</SCHEMEINLINE> takes as arguments a pattern, a data object
    (assertion), and a frame and returns either a one-element stream
    containing the extended frame or <SCHEMEINLINE>the-empty-stream</SCHEMEINLINE> if the match
    fails.

    <SNIPPET>
      <SCHEME>
        (define (check-an-assertion assertion query-pat query-frame)
        (let ((match-result
              (pattern-match query-pat assertion query-frame)))
        (if (eq? match-result 'failed)
              the-empty-stream
              (singleton-stream match-result))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The basic pattern matcher returns either the symbol <SCHEMEINLINE>failed</SCHEMEINLINE> or an
    extension of the given frame.  The basic idea of the matcher is to
    check the pattern against the data, element by element, accumulating
    bindings for the pattern variables.  If the pattern and the data
    object are the same, the match succeeds and we return the frame of
    bindings accumulated so far.  Otherwise, if the pattern is a variable
    we extend the current frame by binding the variable to the data, so
    long as this is consistent with the bindings already in the frame.  If
    the pattern and the data are both pairs, we (recursively) match the
    <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the pattern against the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the data to produce
    a frame; in this frame we then match the <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the pattern
    against the <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the data.  If none of these cases are
    applicable, the match fails and we return the symbol <SCHEMEINLINE>failed</SCHEMEINLINE>.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{pattern-match} -->
        (define (pattern-match pat dat frame)
        (cond ((eq? frame 'failed) 'failed)
              ((equal? pat dat) frame)
              ((var? pat) (extend-if-consistent pat dat frame))
              ((and (pair? pat) (pair? dat))
              (pattern-match (cdr pat)
              (cdr dat)
              (pattern-match (car pat)
              (car dat)
              frame)))
              (else 'failed)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    Here is the
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    that extends a frame by adding a new binding, if
    this is consistent with the bindings already in the frame:

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{extend-if-consistent} -->
        (define (extend-if-consistent var dat frame)
        (let ((binding (binding-in-frame var frame)))
        (if binding
              (pattern-match (binding-value binding) dat frame)
              (extend var dat frame))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    If there is no binding for the variable in the frame, we simply add
    the binding of the variable to the data.  Otherwise we match, in the
    frame, the data against the value of the variable in the frame.  If
    the stored value contains only constants, as it must if it was stored
    during pattern matching by <SCHEMEINLINE>extend-if-consistent</SCHEMEINLINE>, then the match
    simply tests whether the stored and new values are the same.  If so,
    it returns the unmodified frame; if not, it returns a failure
    indication.  The stored value may, however, contain pattern variables
    if it was stored during unification (see section<SPACE/><REF NAME="sec:query-unify"/>).
    The recursive match of the stored pattern against the new data will add or
    check bindings for the variables in this pattern.  For example,
    suppose we have a frame in which <SCHEMEINLINE>?x</SCHEMEINLINE> is bound to <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> and
    <SCHEMEINLINE>?y</SCHEMEINLINE> is unbound, and we wish to augment this frame by a binding of
    <SCHEMEINLINE>?x</SCHEMEINLINE> to <SPLITINLINE><SCHEME><SCHEMEINLINE>(f b)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  We look up <SCHEMEINLINE>?x</SCHEMEINLINE> and find that it is
    bound to <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  This leads us to match <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> against
    the proposed new value <SPLITINLINE><SCHEME><SCHEMEINLINE>(f b)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> in the same frame.  Eventually
    this match extends the frame by adding a binding of <SCHEMEINLINE>?y</SCHEMEINLINE> to <SCHEMEINLINE>b</SCHEMEINLINE>.  <SCHEMEINLINE>?X</SCHEMEINLINE> remains bound to <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  We never modify a stored
    binding and we never store more than one binding for a given variable.
  </TEXT>

  <TEXT>
    The
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    used by <SCHEMEINLINE>extend-if-consistent</SCHEMEINLINE> to manipulate
    bindings are defined in section<SPACE/><REF NAME="sec:query-bindings"/>.
  </TEXT>

  <SUBHEADING>
    <NAME>Patterns with dotted tails</NAME>
  </SUBHEADING>

  <INDEX>dotted-tail notation<SUBINDEX>query@in query pattern</SUBINDEX></INDEX>
  <TEXT>
    If a pattern contains a dot followed by a pattern variable, the
    pattern variable matches the rest of the data list (rather than the
    next element of the data list), just as one would expect with the
    dotted-tail notation described in exercise<SPACE/><REF NAME="ex:dotted-arglist"/>.
    Although the pattern matcher we have just implemented doesn<APOS/>t look for
    dots, it does behave as we want.  This is because the <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE> <SCHEMEINLINE>read</SCHEMEINLINE>
    primitive, which is used by <SCHEMEINLINE>query-driver-loop</SCHEMEINLINE> to read the query
    and represent it as a list structure, treats dots in a special way.
  </TEXT>

  <TEXT>
    <!-- \indprim{read}[dotted-tail notation handling by]-->
    <INDEX>dotted-tail notation<SUBINDEX>read@<SCHEMEINLINE>read</SCHEMEINLINE> and</SUBINDEX></INDEX>
    When <SCHEMEINLINE>read</SCHEMEINLINE> sees a dot, instead of making the next item be the
    next element of a list (the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of a <SPLITINLINE><SCHEME><SCHEMEINLINE>cons</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>pair</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> whose <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>
    will be the rest of the list) it makes the next item be the <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>
    of the list structure.  For example, the list structure produced by
    <SCHEMEINLINE>read</SCHEMEINLINE> for the pattern <SPLITINLINE><SCHEME><SCHEMEINLINE>(computer ?type)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> could be constructed
    by evaluating the expression <SPLITINLINE><SCHEME><SCHEMEINLINE>(cons 'computer (cons<SPACE/>'?type '()))</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>,
    and that for <SPLITINLINE><SCHEME><SCHEMEINLINE>(computer ?type)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> could be constructed by
    evaluating the expression <SPLITINLINE><SCHEME><SCHEMEINLINE>(cons 'computer '?type)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.
  </TEXT>

  <TEXT>
    Thus, as <SCHEMEINLINE>pattern-match</SCHEMEINLINE> recursively compares <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>s and <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>s of a data list and a pattern that had a dot, it eventually
    matches the variable after the dot (which is a <SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the
    pattern) against a sublist of the data list, binding the variable to
    that list.  For example, matching the pattern <SPLITINLINE><SCHEME><SCHEMEINLINE>(computer ?type)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> against
    <SPLITINLINE><SCHEME><SCHEMEINLINE>(computer<SPACE/>programmer<SPACE/>trainee)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> will match <SCHEMEINLINE>?type</SCHEMEINLINE> against the
    list <SPLITINLINE><SCHEME><SCHEMEINLINE>(programmer trainee)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.
    <INDEX>query interpreter<SUBINDEX>pattern matching|)</SUBINDEX></INDEX>
    <INDEX>pattern matching<SUBINDEX>implementation|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Rules and Unification</NAME>

  <LABEL NAME="sec:query-unify"/>
  <TEXT>
    <INDEX>rule (query language)<SUBINDEX>applying|(</SUBINDEX></INDEX>
    <SCHEMEINLINE>Apply-rules</SCHEMEINLINE> is the rule analog of <SCHEMEINLINE>find-assertions</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-match"/>).  It
    takes as input a pattern and a frame, and it forms a stream of
    extension frames by applying rules from the data base.  <SCHEMEINLINE>Stream-flatmap</SCHEMEINLINE> maps <SCHEMEINLINE>apply-a-rule</SCHEMEINLINE> down the stream of possibly
    applicable rules (selected by <SCHEMEINLINE>fetch-rules</SCHEMEINLINE>, section<SPACE/><REF NAME="sec:query-db"/>)
    and combines the resulting streams of frames.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{apply-rules} -->
        (define (apply-rules pattern frame)
        (stream-flatmap (lambda (rule)
              (apply-a-rule rule pattern frame))
              (fetch-rules pattern frame)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Apply-a-rule</SCHEMEINLINE> applies rules using the method outlined in
    section <REF NAME="sec:how-query-works"/>.  It first augments its argument
    frame by unifying the rule conclusion with the pattern in the given
    frame.  If this succeeds, it evaluates the rule body in this new
    frame.
  </TEXT>

  <TEXT>
    Before any of this happens, however, the program renames all the
    variables in the rule with unique new names.  The reason for this is
    to prevent the variables for different rule applications from becoming
    confused with each other.  For instance, if two rules both use a
    variable named <SCHEMEINLINE>?x</SCHEMEINLINE>, then each one may add a binding for <SCHEMEINLINE>?x</SCHEMEINLINE>
    to the frame when it is applied.  These two <SCHEMEINLINE>?x</SCHEMEINLINE><APOS/>s have nothing to
    do with each other, and we should not be fooled into thinking that the
    two bindings must be consistent.  Rather than rename variables, we
    could devise a more clever environment structure; however, the
    renaming approach we have chosen here is the most straightforward,
    even if not the most efficient.  (See
    exercise<SPACE/><REF NAME="ex:query-local-names"/>.)  Here is the <SCHEMEINLINE>apply-a-rule</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>:

    <SNIPPET>
      <SCHEME>
        (define (apply-a-rule rule query-pattern query-frame)
        (let ((clean-rule (rename-variables-in rule)))
        (let ((unify-result
              (unify-match query-pattern
              (conclusion clean-rule)
              query-frame)))
        (if (eq? unify-result 'failed)
              the-empty-stream
              (qeval (rule-body clean-rule)
              (singleton-stream unify-result))))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The selectors <SCHEMEINLINE>rule-body</SCHEMEINLINE> and <SCHEMEINLINE>conclusion</SCHEMEINLINE> that extract parts
    of a rule are defined in section<SPACE/><REF NAME="sec:query-syntax"/>.
  </TEXT>

  <TEXT>
    We generate unique variable names by associating a unique identifier
    (such as a number) with each rule application and combining this
    identifier with the original variable names.  For example, if the
    rule-application identifier is 7, we might change each <SCHEMEINLINE>?x</SCHEMEINLINE> in
    the rule to <SCHEMEINLINE>?x-7</SCHEMEINLINE> and each <SCHEMEINLINE>?y</SCHEMEINLINE> in the rule to <SCHEMEINLINE>?y-7</SCHEMEINLINE>.
    (<SCHEMEINLINE>Make-new-variable</SCHEMEINLINE> and <SCHEMEINLINE>new-rule-application\?id</SCHEMEINLINE> are
    included with the syntax
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    in section<SPACE/><REF NAME="sec:query-syntax"/>.)

    <SNIPPET>
      <SCHEME>
        (define (rename-variables-in rule)
        (let ((rule-application-id (new-rule-application-id)))
        (define (tree-walk exp)
        (cond ((var? exp)
              (make-new-variable exp rule-application-id))
              ((pair? exp)
              (cons (tree-walk (car exp))
              (tree-walk (cdr exp))))
              (else exp)))
        (tree-walk rule)))
      </SCHEME>
    </SNIPPET>
    <INDEX>rule (query language)<SUBINDEX>applying|)</SUBINDEX></INDEX>
  </TEXT>

  <TEXT>
    <INDEX>query interpreter<SUBINDEX>unification|(</SUBINDEX></INDEX>
    <INDEX>unification<SUBINDEX>implementation|(</SUBINDEX></INDEX>
    The unification algorithm is implemented as a
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    that takes as
    inputs two patterns and a frame and returns either the extended frame
    or the symbol <SCHEMEINLINE>failed</SCHEMEINLINE>.
    The unifier is like the pattern matcher except that it is
    symmetrical<EMDASH/>variables are allowed on both sides of the match.
    <SCHEMEINLINE>Unify-match</SCHEMEINLINE> is basically the same as <SCHEMEINLINE>pattern-match</SCHEMEINLINE>,
    except that there is extra code (marked <QUOTE><SCHEMEINLINE>***</SCHEMEINLINE></QUOTE> below) to handle
    the case where the object on the right side of the match is a variable.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{unify-match} -->
        (define (unify-match p1 p2 frame)
        (cond ((eq? frame 'failed) 'failed)
              ((equal? p1 p2) frame)
              ((var? p1) (extend-if-possible p1 p2 frame))
              ((var? p2) (extend-if-possible p2 p1 frame))  <EM>; ***</EM>
              ((and (pair? p1) (pair? p2))
              (unify-match (cdr p1)
              (cdr p2)
              (unify-match (car p1)
              (car p2)
              frame)))
              (else 'failed)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    In unification, as in one-sided pattern matching, we want to accept a
    proposed extension of the frame only if it is consistent with existing
    bindings.  The
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    <SCHEMEINLINE>extend-if-possible</SCHEMEINLINE> used in unification
    is the same as the <SCHEMEINLINE>extend-if-consistent</SCHEMEINLINE> used in pattern matching
    except for two special checks, marked <QUOTE><SCHEMEINLINE>***</SCHEMEINLINE></QUOTE> in the program
    below.  In the first case, if the variable we are trying to match is
    not bound, but the value we are trying to match it with
    is itself a (different) variable, it is
    necessary to check to see if the value is bound, and if so, to match
    its value.  If both parties to the match are unbound, we may bind
    either to the other.
  </TEXT>

  <TEXT>
    The second check deals with attempts to bind a variable to a pattern
    that includes that variable.  Such a situation can occur whenever a
    variable is repeated in both patterns.  Consider, for example,
    unifying the two patterns <SPLITINLINE><SCHEME><SCHEMEINLINE>(?x ?x)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> and <SPLITINLINE><SCHEME><SCHEMEINLINE>(?y ^expression involving^ ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> in a frame where both <SCHEMEINLINE>?x</SCHEMEINLINE>
    and <SCHEMEINLINE>?y</SCHEMEINLINE> are unbound.  First <SCHEMEINLINE>?x</SCHEMEINLINE> is matched
    against <SCHEMEINLINE>?y</SCHEMEINLINE>, making a binding of <SCHEMEINLINE>?x</SCHEMEINLINE> to <SCHEMEINLINE>?y</SCHEMEINLINE>.  Next, the same <SCHEMEINLINE>?x</SCHEMEINLINE> is matched against the given expression
    involving <SCHEMEINLINE>?y</SCHEMEINLINE>.
    Since <SCHEMEINLINE>?x</SCHEMEINLINE> is already bound to <SCHEMEINLINE>?y</SCHEMEINLINE>, this
    results in matching <SCHEMEINLINE>?y</SCHEMEINLINE> against the expression.
    If we think of the
    unifier as finding a set of values for the pattern variables that make
    the patterns the same, then these patterns imply instructions to find
    a <SCHEMEINLINE>?y</SCHEMEINLINE> such that <SCHEMEINLINE>?y</SCHEMEINLINE> is equal to the <SCHEMEINLINE>^expression involving^ ?y</SCHEMEINLINE>.
    There is no general method for solving such
    equations, so we reject such bindings; these cases are recognized by
    the predicate 
    <SCHEMEINLINE>depends-on?</SCHEMEINLINE>.<FOOTNOTE>In general, unifying <SCHEMEINLINE>?y</SCHEMEINLINE> with an expression involving
      <INDEX>fixed point<SUBINDEX>unification and</SUBINDEX></INDEX>
      <SCHEMEINLINE>?y</SCHEMEINLINE> would require our being able to find a fixed point of the
      equation <SCHEMEINLINE>?y</SCHEMEINLINE> <LATEXINLINE>$=$</LATEXINLINE> <SCHEMEINLINE>^expression involving^ ?y</SCHEMEINLINE>.  It is
      sometimes possible to syntactically form an expression that appears to
      be the solution.  For example, <SCHEMEINLINE>?y</SCHEMEINLINE><SPACE/><LATEXINLINE>$=$</LATEXINLINE><SPACE/><SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> seems to have
      the fixed point <SPLITINLINE><SCHEME><SCHEMEINLINE>(f (f (f </SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE><ELLIPSIS/><SCHEMEINLINE>)))</SCHEMEINLINE>, which we can produce by
      beginning with the expression <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> and repeatedly substituting
      <SPLITINLINE><SCHEME><SCHEMEINLINE>(f ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> for <SCHEMEINLINE>?y</SCHEMEINLINE>.  Unfortunately, not every such equation has
      a meaningful fixed point.  The issues that arise here are similar to
      the issues of manipulating 
      <INDEX>infinite series</INDEX>
      infinite series in mathematics.  For
      example, we know that 2 is the solution to the equation <LATEXINLINE>$y = 1 + y/2$</LATEXINLINE>.
      Beginning with the expression <LATEXINLINE>$1 + y/2$</LATEXINLINE> and repeatedly substituting <LATEXINLINE>$1
        + y/2$</LATEXINLINE> for <LATEXINLINE>$y$</LATEXINLINE> gives
      <LATEX>
        \[ 2 = y = 1 + y/2 = 1 + (1+y/2)/2 = 1 + 1/2 + y/4 =\cdots, \]
      </LATEX>

      which leads to
      <LATEX>
        \[ 2 = 1 + 1/2 + 1/4 + 1/8 +\cdots. \]
      </LATEX>

      However, if we try the same manipulation beginning with the
      observation that <LATEXINLINE>$-1$</LATEXINLINE> is the solution to the equation <LATEXINLINE>$y = 1 + 2y$</LATEXINLINE>, we
      obtain
      <LATEX>
        \[ -1 = y = 1 + 2y = 1 + 2(1 + 2y) = 1 + 2 + 4y = \cdots, \]
      </LATEX>

      which leads to 
      <LATEX>
        \[ -1 = 1 + 2 + 4 + 8 +\cdots. \]
      </LATEX>
      Although the formal manipulations used in deriving these two equations
      are identical, the first result is a valid assertion about infinite
      series but the second is not.  Similarly, for our unification results,
      reasoning with an arbitrary syntactically constructed expression may
      lead to errors.</FOOTNOTE>
    On the other hand, we do not want to reject attempts
    to bind a variable to itself.  For example, consider unifying <SPLITINLINE><SCHEME><SCHEMEINLINE>(?x<SPACE/>?x)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>
    and <SPLITINLINE><SCHEME><SCHEMEINLINE>(?y<SPACE/>?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  The second attempt to bind <SCHEMEINLINE>?x</SCHEMEINLINE> to <SCHEMEINLINE>?y</SCHEMEINLINE> matches <SCHEMEINLINE>?y</SCHEMEINLINE> (the stored value of <SCHEMEINLINE>?x</SCHEMEINLINE>) against <SCHEMEINLINE>?y</SCHEMEINLINE>
    (the new value of <SCHEMEINLINE>?x</SCHEMEINLINE>).  This is taken care of by the <SCHEMEINLINE>equal?</SCHEMEINLINE> clause
    of <SCHEMEINLINE>unify-match</SCHEMEINLINE>.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{extend-if-possible} -->
        (define (extend-if-possible var val frame)
        (let ((binding (binding-in-frame var frame)))
        (cond (binding
              (unify-match
              (binding-value binding) val frame))
              ((var? val)                      <EM>; ***</EM>
              (let ((binding (binding-in-frame val frame)))
              (if binding
              (unify-match
              var (binding-value binding) frame)
              (extend var val frame))))
              ((depends-on? val var frame)     <EM>; ***</EM>
              'failed)
              (else (extend var val frame)))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Depends-on?</SCHEMEINLINE> is a predicate that tests whether an expression
    proposed to be the value of a pattern variable depends on the variable.
    This must be done relative to the current frame because the expression
    may contain occurrences of a variable that already has a value that
    depends on our test variable.  The structure of <SCHEMEINLINE>depends-on?</SCHEMEINLINE> is a
    simple recursive tree walk in which we substitute for the values of
    variables whenever necessary.

    <SNIPPET>
      <SCHEME>
        (define (depends-on? exp var frame)
        (define (tree-walk e)
        (cond ((var? e)
              (if (equal? var e)
              true
              (let ((b (binding-in-frame e frame)))
              (if b
              (tree-walk (binding-value b))
              false))))
              ((pair? e)
              (or (tree-walk (car e))
              (tree-walk (cdr e))))
              (else false)))
        (tree-walk exp))
      </SCHEME>
    </SNIPPET>
    <INDEX>query interpreter<SUBINDEX>unification|)</SUBINDEX></INDEX>
    <INDEX>unification<SUBINDEX>implementation|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Maintaining the Data Base</NAME>

  <LABEL NAME="sec:query-db"/>
  <INDEX>query interpreter<SUBINDEX>data base|(</SUBINDEX></INDEX>
  <TEXT>
    <INDEX>data base<SUBINDEX>indexing</SUBINDEX></INDEX><INDEX>indexing a data base</INDEX>
    One important problem in designing logic programming languages is that
    of arranging things so that as few irrelevant data-base entries as
    possible will be examined in checking a given pattern.  In our
    system, in addition to storing all assertions in one big stream,
    we store all assertions whose <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>s are constant symbols
    in separate streams, in a table indexed by the symbol.  To fetch an
    assertion that may match a pattern, we first check to see if the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the pattern is a constant symbol.  If so, we return (to be
    tested using the matcher) all the stored assertions that have the same
    <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  If the pattern<APOS/>s <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> is not a constant symbol, we
    return all the stored assertions.  Cleverer methods could also take
    advantage of information in the frame, or try also to optimize the
    case where the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the pattern is not a constant symbol.  We
    avoid building our criteria for indexing (using the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>,
    handling only the case of constant symbols) into the program; instead
    we call on predicates and selectors that embody our criteria.

    <SNIPPET>
      <SCHEME>
        (define THE-ASSERTIONS the-empty-stream)

        <!--  \indcode*{fetch-assertions} -->
        (define (fetch-assertions pattern frame)
        (if (use-index? pattern)
        (get-indexed-assertions pattern)
        (get-all-assertions)))

        (define (get-all-assertions) THE-ASSERTIONS)

        (define (get-indexed-assertions pattern)
        (get-stream (index-key-of pattern) 'assertion-stream))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Get-stream</SCHEMEINLINE> looks up a stream in the table and returns an empty
    stream if nothing is stored there.

    <SNIPPET>
      <SCHEME>
        (define (get-stream key1 key2)
        (let ((s (get key1 key2)))
        (if s s the-empty-stream)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    Rules are stored similarly, using the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the rule
    conclusion.  Rule conclusions are arbitrary patterns, however, so they
    differ from assertions in that they can contain variables.  A pattern
    whose <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> is a constant symbol can match rules whose conclusions
    start with a variable as well as rules whose conclusions have the same
    <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  Thus, when fetching rules that might match a pattern whose
    <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> is a constant symbol we fetch all rules whose conclusions
    start with a variable as well as those whose conclusions have the same
    <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> as the pattern.  For this purpose we store all rules whose
    conclusions start with a variable in a separate stream in our table,
    indexed by the symbol <SCHEMEINLINE>?</SCHEMEINLINE>.

    <SNIPPET>
      <SCHEME>
        (define THE-RULES the-empty-stream)

        <!--  \indcode*{fetch-rules} -->
        (define (fetch-rules pattern frame)
        (if (use-index? pattern)
        (get-indexed-rules pattern)
        (get-all-rules)))

        (define (get-all-rules) THE-RULES)

        (define (get-indexed-rules pattern)
        (stream-append
        (get-stream (index-key-of pattern) 'rule-stream)
        (get-stream '? 'rule-stream)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Add-rule-or-assertion!</SCHEMEINLINE> is used by <SCHEMEINLINE>query-driver-loop</SCHEMEINLINE> to
    add assertions and rules to the data base.  Each item is stored in the
    index, if appropriate, and in a stream of all assertions or rules in
    the data base.

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{add-rule-or-assertion!} -->
        (define (add-rule-or-assertion! assertion)
        (if (rule? assertion)
        (add-rule! assertion)
        (add-assertion! assertion)))

        (define (add-assertion! assertion)
        (store-assertion-in-index assertion)
        (let ((old-assertions THE-ASSERTIONS))
        (set! THE-ASSERTIONS
              (cons-stream assertion old-assertions))
        'ok))

        (define (add-rule! rule)
        (store-rule-in-index rule)
        (let ((old-rules THE-RULES))
        (set! THE-RULES (cons-stream rule old-rules))
        'ok))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>  
    To actually store an assertion or a rule, we check to see if it can be
    indexed.  If so, we store it in the appropriate stream.

    <SNIPPET>
      <SCHEME>
        (define (store-assertion-in-index assertion)
        (if (indexable? assertion)
        (let ((key (index-key-of assertion)))
              (let ((current-assertion-stream
              (get-stream key 'assertion-stream)))
              (put key
              'assertion-stream
              (cons-stream assertion
              current-assertion-stream))))))

        (define (store-rule-in-index rule)
        (let ((pattern (conclusion rule)))
        (if (indexable? pattern)
              (let ((key (index-key-of pattern)))
              (let ((current-rule-stream
              (get-stream key 'rule-stream)))
              (put key
              'rule-stream
              (cons-stream rule
              current-rule-stream)))))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The following
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    define how the data-base index is used.  A
    pattern (an assertion or a rule conclusion) will be stored in the
    table if it starts with a variable or a constant symbol.

    <SNIPPET>
      <SCHEME>
        (define (indexable? pat)
        (or (constant-symbol? (car pat))
        (var? (car pat))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The key under which a pattern is stored in the table is either <SCHEMEINLINE>?</SCHEMEINLINE> (if it starts with a variable) or the constant symbol with which
    it starts.

    <SNIPPET>
      <SCHEME>
        (define (index-key-of pat)
        (let ((key (car pat)))
        (if (var? key) '? key)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The index will be used to retrieve items that might match a pattern if
    the pattern starts with a constant symbol.

    <SNIPPET>
      <SCHEME>
        (define (use-index? pat)
        (constant-symbol? (car pat)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <EXERCISE>
    What is the purpose of the <SCHEMEINLINE>let</SCHEMEINLINE> bindings in the
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    <SCHEMEINLINE>add-assertion!</SCHEMEINLINE> and <SCHEMEINLINE>add-rule!</SCHEMEINLINE><LATEXINLINE>$\,$</LATEXINLINE>?  What would be wrong with the
    following implementation of <SCHEMEINLINE>add-assertion!</SCHEMEINLINE><LATEXINLINE>$\,$</LATEXINLINE>?
    Hint: Recall the definition of the infinite stream of ones in
    section<SPACE/><REF NAME="sec:infinite-streams"/>: <SPLITINLINE><SCHEME><SCHEMEINLINE>(define ones (cons-stream 1 ones))</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.

    <SNIPPET>
      <SCHEME>
        (define (add-assertion! assertion)
        (store-assertion-in-index assertion)
        (set! THE-ASSERTIONS
              (cons-stream assertion THE-ASSERTIONS))
        'ok)
      </SCHEME>
    </SNIPPET>
  </EXERCISE>
  <INDEX>query interpreter<SUBINDEX>data base|)</SUBINDEX></INDEX>
      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Stream Operations</NAME>

  <LABEL NAME="sec:query-streams"/>
  <INDEX>query interpreter<SUBINDEX>stream operations|(</SUBINDEX></INDEX>
  <TEXT>
    The query system uses a few stream operations that were not presented
    in chapter<SPACE/>3.
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Stream-append-delayed</SCHEMEINLINE> and <SCHEMEINLINE>interleave-delayed</SCHEMEINLINE>
    are just like <SCHEMEINLINE>stream\?append</SCHEMEINLINE> and <SCHEMEINLINE>interleave</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:exploiting-streams"/>),
    except that they take a delayed argument (like the <SCHEMEINLINE>integral</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    in section<SPACE/><REF NAME="sec:streams-and-delayed-evaluation"/>).
    This postpones looping in some cases (see exercise<SPACE/><REF NAME="ex:q-why-not-append"/>).

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{stream-append-delayed} -->
        (define (stream-append-delayed s1 delayed-s2)
        (if (stream-null? s1)
        (force delayed-s2)
        (cons-stream
        (stream-car s1)
        (stream-append-delayed (stream-cdr s1) delayed-s2))))

        <!--  \indcode*{interleave-delayed} -->
        (define (interleave-delayed s1 delayed-s2)
        (if (stream-null? s1)
        (force delayed-s2)
        (cons-stream
        (stream-car s1)
        (interleave-delayed (force delayed-s2)
              (delay (stream-cdr s1))))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <SCHEMEINLINE>Stream-flatmap</SCHEMEINLINE>, which is used throughout the query evaluator to
    map a
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    over a stream of frames and combine the resulting
    streams of frames, is the stream analog of the <SCHEMEINLINE>flatmap</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    introduced for ordinary lists in section<SPACE/><REF NAME="sec:nested-mappings"/>.
    Unlike ordinary <SCHEMEINLINE>flatmap</SCHEMEINLINE>, however, we accumulate the streams with
    an interleaving process, rather than simply appending them (see
    exercises<SPACE/><REF NAME="ex:q-why-interleave"/> and <SPACE/><REF NAME="ex:q-why-delay"/>).

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{stream-flatmap} -->
        (define (stream-flatmap proc s)
        (flatten-stream (stream-map proc s)))

        <!--  \indcode*{flatten-stream} -->
        (define (flatten-stream stream)
        (if (stream-null? stream)
        the-empty-stream
        (interleave-delayed
        (stream-car stream)
        (delay (flatten-stream (stream-cdr stream))))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The evaluator also uses the following simple
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    to generate a stream consisting of a single element:

    <SNIPPET>
      <SCHEME>
        <!--  \indcode*{singleton-stream} -->
        (define (singleton-stream x)
        (cons-stream x the-empty-stream))
      </SCHEME>
    </SNIPPET>
    <INDEX>query interpreter<SUBINDEX>stream operations|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Query Syntax
    <SPLITINLINE><SCHEME>Procedures</SCHEME><JAVASCRIPT>Functions</JAVASCRIPT></SPLITINLINE></NAME>

  <LABEL NAME="sec:query-syntax"/>
  <INDEX>query interpreter<SUBINDEX>syntax of query language|(</SUBINDEX></INDEX>
  <TEXT>
    <SCHEMEINLINE>Type</SCHEMEINLINE> and <SCHEMEINLINE>contents</SCHEMEINLINE>, used by <SCHEMEINLINE>qeval</SCHEMEINLINE>
    (section<SPACE/><REF NAME="sec:query-eval"/>), specify that a
    special form is identified by the symbol in its <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.
    They are the same as the <SCHEMEINLINE>type-tag</SCHEMEINLINE> and <SCHEMEINLINE>contents</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    in
    section<SPACE/><REF NAME="sec:manifest-types"/>, except for the error message.

    <SNIPPET>
      <SCHEME>
        (define (type exp)
        (if (pair? exp)
        (car exp)
        (error "Unknown expression TYPE" exp)))

        (define (contents exp)
        (if (pair? exp)
        (cdr exp)
        (error "Unknown expression CONTENTS" exp)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The following
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>, used by <SCHEMEINLINE>query-driver-loop</SCHEMEINLINE>
    (in section <REF NAME="sec:query-driver"/>), specify
    that rules and assertions are added to the data base by expressions of
    the form <SPLITINLINE><SCHEME><SCHEMEINLINE>(assert! ^rule-or-assertion^)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>:

    <SNIPPET>
      <SCHEME>
        (define (assertion-to-be-added? exp)
        (eq? (type exp) 'assert!))

        (define (add-assertion-body exp)
        (car (contents exp)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    Here are the syntax definitions for the <SCHEMEINLINE>and</SCHEMEINLINE>,<LATEXINLINE>$\,$</LATEXINLINE> <SCHEMEINLINE>or</SCHEMEINLINE>,<LATEXINLINE>$\,$</LATEXINLINE> <SCHEMEINLINE>not</SCHEMEINLINE>, and <SCHEMEINLINE>lisp-value</SCHEMEINLINE> special forms
    (section<SPACE/><REF NAME="sec:query-eval"/>):

    <SNIPPET>
      <SCHEME>
        (define (empty-conjunction? exps) (null? exps))
        (define (first-conjunct exps) (car exps))
        (define (rest-conjuncts exps) (cdr exps))

        (define (empty-disjunction? exps) (null? exps))
        (define (first-disjunct exps) (car exps))
        (define (rest-disjuncts exps) (cdr exps))

        (define (negated-query exps) (car exps))

        (define (predicate exps) (car exps))
        (define (args exps) (cdr exps))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The following three
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    define the syntax of rules:

    <SNIPPET>
      <SCHEME>
        (define (rule? statement)
        (tagged-list? statement 'rule))

        (define (conclusion rule) (cadr rule))

        (define (rule-body rule)
        (if (null? (cddr rule))
        '(always-true)
        (caddr rule)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    <INDEX>query interpreter<SUBINDEX>pattern-variable representation|(</SUBINDEX></INDEX>
    <INDEX>pattern variable<SUBINDEX>representation of|(</SUBINDEX></INDEX>
    <SCHEMEINLINE>Query-driver-loop</SCHEMEINLINE> (section<SPACE/><REF NAME="sec:query-driver"/>)
    calls <SCHEMEINLINE>query-syntax-process</SCHEMEINLINE> to
    transform pattern variables in the expression, which have the form
    <SCHEMEINLINE>?symbol</SCHEMEINLINE>, into the internal format <SPLITINLINE><SCHEME><SCHEMEINLINE>(? symbol)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  That is to
    say, a pattern such as <SPLITINLINE><SCHEME><SCHEMEINLINE>(job ?x ?y)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> is actually represented
    internally by the system as <SPLITINLINE><SCHEME><SCHEMEINLINE>(job (? x) (? y))</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>.  This increases
    the efficiency of query processing, since it means that the system can check to see if an expression is a pattern variable by checking
    whether the <SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> of the expression is the symbol <SCHEMEINLINE>?</SCHEMEINLINE>, rather
    than having to extract characters from the symbol.  The syntax
    transformation is accomplished by the following
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>:<FOOTNOTE>Most <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE> systems give the user the ability to
      modify the ordinary <SCHEMEINLINE>read</SCHEMEINLINE>
      <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
      to perform such
      transformations by defining 
      <INDEX>reader macro character</INDEX>
      <!-- \indprim{read}[macro characters]-->
      <!-- \indcodeplus{'}{(single quote)}[read@<SCHEMEINLINE>read</SCHEMEINLINE> and]-->
      <!-- \indsf{quote}[read@<SCHEMEINLINE>read</SCHEMEINLINE> and]-->
      <EM>reader macro characters</EM>.  Quoted
      expressions are already handled in this way: The reader automatically
      translates <SCHEMEINLINE>'expression</SCHEMEINLINE> into <SPLITINLINE><SCHEME><SCHEMEINLINE>(quote expression)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> before the
      evaluator sees it.  We could arrange for <SCHEMEINLINE>?expression</SCHEMEINLINE> to be
      transformed into <SPLITINLINE><SCHEME><SCHEMEINLINE>(? expression)</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>???</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE> in the same way; however, for
      the sake of clarity we have included the transformation
      <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
      here
      explicitly.

      <INDEX>character strings<SUBINDEX>primitive
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    for</SUBINDEX></INDEX>
      <SCHEMEINLINE>Expand-question-mark</SCHEMEINLINE> and <SCHEMEINLINE>contract-question-mark</SCHEMEINLINE> use
      several
      <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
      with <SCHEMEINLINE>string</SCHEMEINLINE> in their names.
      These are Scheme primitives.
    </FOOTNOTE>

    <SNIPPET>
      <SCHEME>
        (define (query-syntax-process exp)
        (map-over-symbols expand-question-mark exp))

        <!--  \indcode*{map-over-symbols} -->
        (define (map-over-symbols proc exp)
        (cond ((pair? exp)
              (cons (map-over-symbols proc (car exp))
              (map-over-symbols proc (cdr exp))))
              ((symbol? exp) (proc exp))
              (else exp)))

        (define (expand-question-mark symbol)
        (let ((chars (symbol-&gt;string symbol)))
        (if (string=? (substring chars 0 1) "?")
              (list '?
              (string-&gt;symbol
              (substring chars 1 (string-length chars))))
              symbol)))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    Once the variables are transformed in this way, the variables in a
    pattern are lists starting with <SCHEMEINLINE>?</SCHEMEINLINE>,
    and the constant symbols (which need to be recognized for
    data-base indexing, section<SPACE/><REF NAME="sec:query-db"/>) are just the symbols.

    <SNIPPET>
      <SCHEME>
        (define (var? exp)
        (tagged-list? exp '?))

        (define (constant-symbol? exp) (symbol? exp))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    Unique variables are constructed during rule application
    (in section <REF NAME="sec:query-unify"/>) by means of
    the following
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>.  The unique identifier for a rule
    application is a number, which is incremented each time a rule is
    applied.

    <SNIPPET>
      <SCHEME>
        (define rule-counter 0)

        (define (new-rule-application-id)
        (set! rule-counter (+ 1 rule-counter))
        rule-counter)

        (define (make-new-variable var rule-application-id)
        (cons '? (cons rule-application-id (cdr var))))
      </SCHEME>
    </SNIPPET>
  </TEXT>

  <TEXT>
    When <SCHEMEINLINE>query-driver-loop</SCHEMEINLINE> instantiates the query to print the
    answer, it converts any unbound pattern variables back to the right
    form for printing, using

    <SNIPPET>
      <SCHEME>
        (define (contract-question-mark variable)
        (string-&gt;symbol
        (string-append "?" 
        (if (number? (cadr variable))
              (string-append (symbol-&gt;string (caddr variable))
              "-"
              (number-&gt;string (cadr variable)))
              (symbol-&gt;string (cadr variable))))))
      </SCHEME>
    </SNIPPET>
    <INDEX>query interpreter<SUBINDEX>pattern-variable representation|)</SUBINDEX></INDEX>
    <INDEX>pattern variable<SUBINDEX>representation of|)</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>syntax of query language|)</SUBINDEX></INDEX>
  </TEXT>

      </SUBSUBSECTION>

      <SUBSUBSECTION>
  <NAME>Frames and Bindings</NAME>

  <LABEL NAME="sec:query-bindings"/>

  <TEXT>
    <INDEX>query interpreter<SUBINDEX>frame|(</SUBINDEX></INDEX>
    <INDEX>frame (query interpreter)<SUBINDEX>representation</SUBINDEX></INDEX>
    Frames are represented as lists of bindings, which are
    variable-value pairs:

    <SNIPPET>
      <SCHEME>
        (define (make-binding variable value)
        (cons variable value))

        (define (binding-variable binding)
        (car binding))

        (define (binding-value binding)
        (cdr binding))

        (define (binding-in-frame variable frame)
        (assoc variable frame))

        (define (extend variable value frame)
        (cons (make-binding variable value) frame))
      </SCHEME>
    </SNIPPET>
    <INDEX>query interpreter<SUBINDEX>frame|)</SUBINDEX></INDEX>
  </TEXT>

  <EXERCISE>
    Louis Reasoner wonders why the <SCHEMEINLINE>simple-query</SCHEMEINLINE> and <SCHEMEINLINE>disjoin</SCHEMEINLINE>
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>
    (section<SPACE/><REF NAME="sec:query-eval"/>) are implemented using
    explicit <SCHEMEINLINE>delay</SCHEMEINLINE> operations, rather than being defined as follows:

    <SNIPPET>
      <SCHEME>
        (define (simple-query query-pattern frame-stream)
        (stream-flatmap
        (lambda (frame)
        (stream-append (find-assertions query-pattern frame)
              (apply-rules query-pattern frame)))
        frame-stream))

        (define (disjoin disjuncts frame-stream)
        (if (empty-disjunction? disjuncts)
        the-empty-stream
        (interleave
        (qeval (first-disjunct disjuncts) frame-stream)
        (disjoin (rest-disjuncts disjuncts) frame-stream))))
      </SCHEME>
    </SNIPPET>
    Can you give examples of queries where these simpler definitions would
    lead to undesirable behavior?
    <LABEL NAME="ex:q-why-not-append"/>
  </EXERCISE>

  <EXERCISE>
    Why do <SCHEMEINLINE>disjoin</SCHEMEINLINE> and <SCHEMEINLINE>stream-flatmap</SCHEMEINLINE> interleave the
    streams rather than simply append them?  Give examples that illustrate
    why interleaving works better.  (Hint: Why did we use <SCHEMEINLINE>interleave</SCHEMEINLINE> in
    section<SPACE/><REF NAME="sec:exploiting-streams"/>?)
    <LABEL NAME="ex:q-why-interleave"/>
  </EXERCISE>

  <EXERCISE><LABEL NAME="ex:q-why-delay"/>
    Why does <SCHEMEINLINE>flatten-stream</SCHEMEINLINE> use <SCHEMEINLINE>delay</SCHEMEINLINE> explicitly?
    What would be wrong with defining it as follows:
    <SNIPPET>
      <SCHEME>
        (define (flatten-stream stream)
        (if (stream-null? stream)
        the-empty-stream
        (interleave
        (stream-car stream)
        (flatten-stream (stream-cdr stream)))))
      </SCHEME>
    </SNIPPET>
  </EXERCISE>

  <EXERCISE>
    <!--  \indcode{stream-flatmap} -->
    Alyssa P. Hacker proposes to use a simpler version of <SCHEMEINLINE>stream-flatmap</SCHEMEINLINE>
    in <SCHEMEINLINE>negate</SCHEMEINLINE>, <SCHEMEINLINE>lisp-value</SCHEMEINLINE>, and <SCHEMEINLINE>find-assertions</SCHEMEINLINE>.
    She observes that the
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    that is mapped over the frame stream
    in these cases always produces either the empty stream or a singleton
    stream, so no interleaving is needed when combining these streams.
    <OL>
      <LI>
        Fill in the missing expressions in Alyssa<APOS/>s program.
        <SNIPPET EVAL="no">
    <SCHEME>
      (define (simple-stream-flatmap proc s)
      (simple-flatten (stream-map proc s)))

      (define (simple-flatten stream)
      (stream-map ^??^
      (stream-filter ^??^ stream)))
    </SCHEME>
        </SNIPPET>
      </LI>
      <LI>
        Does the query system<APOS/>s behavior change if we change it in this way?
      </LI>
    </OL>
  </EXERCISE>

  <EXERCISE>
    <INDEX>query language<SUBINDEX>extensions to</SUBINDEX></INDEX>
    <INDEX>compound query<SUBINDEX>processing</SUBINDEX></INDEX>
    <!-- \indcodeplus{unique}{(query language)}-->
    Implement for the query language a new special form called <SCHEMEINLINE>unique</SCHEMEINLINE>.  <SCHEMEINLINE>Unique</SCHEMEINLINE> should succeed if there is precisely one item
    in the data base satisfying a specified query.  For example,

    <SNIPPET>
      <SCHEME>
        (unique (job ?x (computer wizard)))
      </SCHEME>
    </SNIPPET>

    should print the one-item stream

    <SNIPPET>
      <SCHEME>
        (unique (job (Bitdiddle Ben) (computer wizard)))
      </SCHEME>
    </SNIPPET>

    since Ben is the only computer wizard, and

    <SNIPPET>
      <SCHEME>
        (unique (job ?x (computer programmer)))
      </SCHEME>
    </SNIPPET>

    should print the empty stream, since there is more than one computer
    programmer.  Moreover,

    <SNIPPET>
      <SCHEME>
        (and (job ?x ?j) (unique (job ?anyone ?j)))
      </SCHEME>
    </SNIPPET>

    should list all the jobs that are filled by only one person, and the
    people who fill them.

    There are two parts to implementing <SCHEMEINLINE>unique</SCHEMEINLINE>.  The first is to
    write a
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    that handles this special form, and the second is to make
    <SCHEMEINLINE>qeval</SCHEMEINLINE> dispatch to that
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>.  The second part is trivial,
    since <SCHEMEINLINE>qeval</SCHEMEINLINE> does its dispatching in a data-directed way.  If
    your
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    is called <SCHEMEINLINE>uniquely-asserted</SCHEMEINLINE>, all you need to do
    is

    <SNIPPET>
      <SCHEME>
        (put 'unique 'qeval uniquely-asserted)
      </SCHEME>
    </SNIPPET>

    and <SCHEMEINLINE>qeval</SCHEMEINLINE> will dispatch to this
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    for every query whose
    <SCHEMEINLINE>type</SCHEMEINLINE> (<SPLITINLINE><SCHEME><SCHEMEINLINE>car</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>head</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>) is the symbol <SCHEMEINLINE>unique</SCHEMEINLINE>.

    The real problem is to write the
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    <SCHEMEINLINE>uniquely-asserted</SCHEMEINLINE>.
    This should take as input the <SCHEMEINLINE>contents</SCHEMEINLINE> (<SPLITINLINE><SCHEME><SCHEMEINLINE>cdr</SCHEMEINLINE></SCHEME><JAVASCRIPT><JAVASCRIPTINLINE>tail</JAVASCRIPTINLINE></JAVASCRIPT></SPLITINLINE>) of the <SCHEMEINLINE>unique</SCHEMEINLINE> query, together with a stream of frames.  For each frame in
    the stream, it should use <SCHEMEINLINE>qeval</SCHEMEINLINE> to find the stream of all
    extensions to the frame that satisfy the given query.  Any stream that
    does not have exactly one item in it should be eliminated.  The
    remaining streams should be passed back to be accumulated into one big
    stream that is the result of the <SCHEMEINLINE>unique</SCHEMEINLINE> query.  This is similar
    to the implementation of the <SCHEMEINLINE>not</SCHEMEINLINE> special form.

    Test your implementation by forming a query that lists all people who
    supervise precisely one person.
    <LABEL NAME="ex:unique"/>
  </EXERCISE>

  <EXERCISE>
    <INDEX>query interpreter<SUBINDEX>improvements to</SUBINDEX></INDEX>
    <!-- \indcodeplus{and}{(query language)}[evaluation of]-->
    <INDEX>compound query<SUBINDEX>processing</SUBINDEX></INDEX>
    Our implementation of <SCHEMEINLINE>and</SCHEMEINLINE> as a series combination of queries
    (figure<SPACE/><REF NAME="fig:query-and"/>) is elegant, but it is inefficient because in
    processing the second query of the <SCHEMEINLINE>and</SCHEMEINLINE> we must scan the data
    base for each frame produced by the first query.  If the data base has
    <LATEXINLINE>$N$</LATEXINLINE> elements, and a typical query produces a number of output frames
    proportional to <LATEXINLINE>$N$</LATEXINLINE> (say <LATEXINLINE>$N/k$</LATEXINLINE>), then scanning the data base for each
    frame produced by the first query will require <LATEXINLINE>$N^{2}/k$</LATEXINLINE> calls to the
    pattern matcher.  Another approach would be to process the two clauses
    of the <SCHEMEINLINE>and</SCHEMEINLINE> separately, then look for all pairs of output frames
    that are compatible.  If each query produces <LATEXINLINE>$N/k$</LATEXINLINE> output frames, then
    this means that we must perform <LATEXINLINE>$N^{2}/k^{2}$</LATEXINLINE> compatibility checks<EMDASH/>a
    factor of <LATEXINLINE>$k$</LATEXINLINE> fewer than the number of matches required in our current
    method.

    Devise an implementation of <SCHEMEINLINE>and</SCHEMEINLINE> that uses this strategy.  You
    must implement a
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    that takes two frames as inputs, checks
    whether the bindings in the frames are compatible, and, if so,
    produces a frame that merges the two sets of bindings.  This operation
    is similar to unification.
    <LABEL NAME="ex:q-exponential-and"/>
  </EXERCISE>

  <EXERCISE>
    <INDEX>query interpreter<SUBINDEX>improvements to</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>problems with <SCHEMEINLINE>not</SCHEMEINLINE> and <SCHEMEINLINE>lisp-value</SCHEMEINLINE></SUBINDEX></INDEX>
    <INDEX>compound query<SUBINDEX>processing</SUBINDEX></INDEX>
    <!-- \indcodeplus{not}{(query language)}[evaluation of]-->
    <!-- \indcodeplus{lisp-value}{(query language)}[evaluation of]-->
    In section<SPACE/><REF NAME="sec:math-logic"/> we saw that <SCHEMEINLINE>not</SCHEMEINLINE> and <SCHEMEINLINE>lisp-value</SCHEMEINLINE> can cause the query language to give <QUOTE>wrong</QUOTE> answers if
    these filtering operations are applied to frames in which variables
    are unbound.  Devise a way to fix this shortcoming.  One idea is to
    perform the filtering in a <QUOTE>delayed</QUOTE> manner by appending to the
    frame a <QUOTE>promise</QUOTE> to filter that is fulfilled only when enough
    variables have been bound to make the operation possible.  We could
    wait to perform filtering until all other operations have been
    performed.  However, for efficiency<APOS/>s sake, we would like to perform
    filtering as soon as possible so as to cut down on the number of
    intermediate frames generated.
    <LABEL NAME="ex:not-query-filter"/>
  </EXERCISE>

  <EXERCISE>
    <INDEX>nondeterministic programming vs.<SPACE/>Scheme programming</INDEX>
    Redesign the query language as a nondeterministic program to be
    implemented using the evaluator of
    section<SPACE/><REF NAME="sec:nondeterministic-evaluation"/>, rather than as a stream
    process.  In this approach, each query will produce a single answer
    (rather than the stream of all answers) and the user can type <SCHEMEINLINE>try-again</SCHEMEINLINE> to see more answers.  You should find that much of the
    mechanism we built in this section is subsumed by nondeterministic
    search and backtracking.  You will probably also find, however, that
    your new query language has subtle differences in behavior from the
    one implemented here.  Can you find examples that illustrate this
    difference?
    <LABEL NAME="ex:query-lang-amb"/>
  </EXERCISE>


  <EXERCISE>
    <INDEX>environment<SUBINDEX>renaming vs.</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX><SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE> interpreter vs.</SUBINDEX></INDEX>
    When we implemented the <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE> evaluator in section<SPACE/><REF NAME="sec:mc-eval"/>,
    we saw how to use local environments to avoid name conflicts between
    the parameters of
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>.  For example, in evaluating

    <SNIPPET>
      <SCHEME>
        (define (square x)
        (* x x))

        (define (sum-of-squares x y)
        (+ (square x) (square y)))

        (sum-of-squares 3 4)
      </SCHEME>
    </SNIPPET>

    there is no confusion between the <SCHEMEINLINE>x</SCHEMEINLINE> in <SCHEMEINLINE>square</SCHEMEINLINE> and the <SCHEMEINLINE>x</SCHEMEINLINE>
    in <SCHEMEINLINE>sum-of-squares</SCHEMEINLINE>, because we evaluate the body of each
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    in an environment that is specially constructed to contain
    bindings for the local variables.  In the query system, we used a
    different strategy to avoid name conflicts in applying rules.  Each
    time we apply a rule we rename the variables with new names that are
    guaranteed to be unique.  The analogous strategy for the <SPLITINLINE><SCHEME>Lisp</SCHEME><JAVASCRIPT>JavaScript</JAVASCRIPT></SPLITINLINE>
    evaluator would be to do away with local environments and simply
    rename the variables in the body of a
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>
    each time we apply the
    <SPLITINLINE><SCHEME>procedure</SCHEME><JAVASCRIPT>function</JAVASCRIPT></SPLITINLINE>.

    <INDEX>block structure<SUBINDEX>query@in query language</SUBINDEX></INDEX>
    <INDEX>environment<SUBINDEX>query@in query interpreter</SUBINDEX></INDEX>
    <INDEX>query interpreter<SUBINDEX>environment structure in</SUBINDEX></INDEX>
    <INDEX>rule (query language)<SUBINDEX>applying</SUBINDEX></INDEX>
    Implement for the query language a rule-application method that uses
    environments rather than renaming.  See if you can build on your
    environment structure to create constructs in the query language for
    dealing with large systems, such as the rule analog of
    block-structured
    <SPLITINLINE><SCHEME>procedures</SCHEME><JAVASCRIPT>functions</JAVASCRIPT></SPLITINLINE>.  Can you relate any of this to the
    problem of making deductions in a context (e.g., <QUOTE>If I supposed that
      <LATEXINLINE>$P$</LATEXINLINE> were true, then I would be able to deduce <LATEXINLINE>$A$</LATEXINLINE> and <LATEXINLINE>$B$</LATEXINLINE>.</QUOTE>) as a
    method of problem solving?  (This problem is open-ended.  A good
    answer is probably worth a Ph.D.)
    <LABEL NAME="ex:query-local-names"/>
  </EXERCISE>
      </SUBSUBSECTION>
    </SUBSECTION>
