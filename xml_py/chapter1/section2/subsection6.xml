<SUBSECTION>

  <NAME>Example: Testing for Primality</NAME>

  <LABEL NAME="sec:primality"/>
  <INDEX>prime number(s)<SUBINDEX>testing for<OPEN/></SUBINDEX></INDEX>
  <INDEX>prime number(s)<OPEN/></INDEX>

  <TEXT>
    This section describes two methods for checking the primality of an
    integer <LATEXINLINE>$n$</LATEXINLINE>, one with order of growth
    <LATEXINLINE>$\Theta(\sqrt{n})$</LATEXINLINE>, and a
    <QUOTE>probabilistic</QUOTE> algorithm with order of growth
    <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE>.  The exercises at the end of
    this section suggest programming projects based on these algorithms.
  </TEXT>

  <SUBHEADING>
    <NAME>Searching for divisors</NAME>
  </SUBHEADING>

  <TEXT>
    Since ancient times, mathematicians have been fascinated by problems
    concerning prime numbers, and many people have worked on the problem
    of determining ways to test if numbers are prime.  One way
    to test if a number is prime is to find the number<APOS/>s divisors.  The
    following program finds the smallest integral divisor (greater than 1)
    of a given number<SPACE/><LATEXINLINE>$n$</LATEXINLINE>.  It does this in a
    straightforward way, by testing <LATEXINLINE>$n$</LATEXINLINE> for
    divisibility by successive integers starting with 2.
    <SNIPPET PAGE="50">
      <INDEX><DECLARATION>find_divisor</DECLARATION></INDEX>
      <INDEX><DECLARATION>divides</DECLARATION></INDEX>
      <INDEX><DECLARATION>smallest_divisor</DECLARATION></INDEX>
      <NAME>smallest_divisor_definition</NAME>
      <REQUIRES>square_definition</REQUIRES>
      <EXAMPLE>smallest_divisor_example</EXAMPLE>
      <SCHEME>
(define (smallest-divisor n)
  (find-divisor n 2))

(define (find-divisor n test-divisor)
  (cond ((&gt; (square test-divisor) n) n)
        ((divides? test-divisor n) test-divisor)
        (else (find-divisor n (+ test-divisor 1)))))

(define (divides? a b)
  (= (remainder b a) 0))
      </SCHEME>
      <PYTHON>
def smallest_divisor(n):
    return find_divisor(n, 2)

def find_divisor(n, test_divisor):
    return (n if square(test_divisor) &gt; n
            else test_divisor if divides(test_divisor, n)
            else find_divisor(n, test_divisor + 1))

def divides(a, b):
    return b % a == 0
      </PYTHON>
    </SNIPPET>
    <SNIPPET PAGE="50" HIDE="yes">
      <NAME>smallest_divisor_example</NAME>
      <REQUIRES>smallest_divisor_definition</REQUIRES>
      <EXPECTED>2</EXPECTED>
      <SCHEME>
(smallest-divisor 42)
      </SCHEME>
      <PYTHON>
print(smallest_divisor(42))
      </PYTHON>
    </SNIPPET>
  </TEXT>

  <TEXT>
    We can test whether a number is prime as follows:
    <LATEXINLINE>$n$</LATEXINLINE> is prime if and only if
    <LATEXINLINE>$n$</LATEXINLINE> is its own smallest divisor.
    <SNIPPET PAGE="50">
      <INDEX><DECLARATION>is_prime</DECLARATION></INDEX>
      <NAME>prime_definition</NAME>
      <REQUIRES>smallest_divisor_definition</REQUIRES>
      <EXAMPLE>prime_example</EXAMPLE>
      <SCHEME>
(define (prime? n)
  (= n (smallest-divisor n)))
      </SCHEME>
      <PYTHON>
def is_prime(n):
    return n == smallest_divisor(n)
      </PYTHON>
    </SNIPPET>

    <SNIPPET PAGE="50" HIDE="yes">
      <NAME>prime_example</NAME>
      <REQUIRES>prime_definition</REQUIRES>
      <EXPECTED>false</EXPECTED>
      <SCHEME>
(prime? 42)
      </SCHEME>
      <PYTHON>
print(is_prime(42))
      </PYTHON>
    </SNIPPET>

  </TEXT>

  <TEXT>
    The end test for 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>find-divisor</SCHEMEINLINE></SCHEME>
      <PYTHON><PYTHONINLINE>find_divisor</PYTHONINLINE></PYTHON>
    </SPLITINLINE>
    is based on the fact that if <LATEXINLINE>$n$</LATEXINLINE> is not prime it
    must have a divisor less than or equal to
    <LATEXINLINE>$\sqrt{n}$</LATEXINLINE>.<FOOTNOTE>If 
    <LATEXINLINE>$d$</LATEXINLINE> is a divisor of 
    <LATEXINLINE>$n$</LATEXINLINE>, then so is <LATEXINLINE>$n/d$</LATEXINLINE>.
    But <LATEXINLINE>$d$</LATEXINLINE> and <LATEXINLINE>$n/d$</LATEXINLINE>
    cannot both be greater than <LATEXINLINE>$\sqrt{n}$</LATEXINLINE>.</FOOTNOTE>
    This means that the algorithm need only test divisors between 1 and
    <LATEXINLINE>$\sqrt{n}$</LATEXINLINE>.  Consequently, the number of steps
    required to identify <LATEXINLINE>$n$</LATEXINLINE> as prime will have order
    of growth <LATEXINLINE>$\Theta(\sqrt{n})$</LATEXINLINE>.
  </TEXT>

  <SUBHEADING>
    <NAME>The Fermat test</NAME>
  </SUBHEADING>

  <TEXT>
    The <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE> primality test is based on
    a result from number theory known as
    <INDEX>Fermat test for primality<OPEN/></INDEX>
    <INDEX>prime number(s)<SUBINDEX>Fermat test for<OPEN/></SUBINDEX></INDEX>
    Fermat<APOS/>s Little
    Theorem.<FOOTNOTE>Pierre
    <INDEX><ORDER>Fermat</ORDER>Fermat, Pierre de</INDEX>
    de Fermat (1601<ENDASH/>1665) is considered to be
    the founder of modern
    <INDEX>number theory</INDEX>
    number theory.  He obtained many important number-theoretic results,
    but he usually announced just the results, without providing his proofs.  
    <INDEX><ORDER>Fermats</ORDER>Fermat<APOS/>s Little Theorem<SUBINDEX>proof</SUBINDEX></INDEX>
    Fermat<APOS/>s Little Theorem was stated in a letter he wrote in 1640.
    The first published proof was given by 
    <INDEX>Euler, Leonhard<SUBINDEX>proof of Fermat<APOS/>s Little Theorem</SUBINDEX></INDEX>
    Euler in 1736 (and an
    earlier, identical proof was discovered in the unpublished manuscripts
    of
    <INDEX>Leibniz, Baron Gottfried Wilhelm von<SUBINDEX>proof of Fermat<APOS/>s Little Theorem</SUBINDEX></INDEX>
    Leibniz).  The most famous of Fermat<APOS/>s results<EMDASH/>known as
    Fermat<APOS/>s Last Theorem<EMDASH/>was jotted down in 1637 in his copy of
    the book <EM>Arithmetic</EM> (by the third-century Greek mathematician 
    <INDEX>Diophantus<APOS/>s <EM>Arithmetic</EM>, Fermat<APOS/>s copy of</INDEX>
    Diophantus) with the remark <QUOTE>I have discovered a truly remarkable
    proof, but this margin is too small to contain it.</QUOTE>  Finding a proof
    of Fermat<APOS/>s Last Theorem became one of the most famous challenges in
    number theory.  A complete
    solution was finally given in 1995 by
    <INDEX>Wiles, Andrew</INDEX>
    Andrew Wiles of Princeton
    University.</FOOTNOTE>

    <BLOCKQUOTE>
      <B>Fermat<APOS/>s Little Theorem:</B> 
      <INDEX><ORDER>Fermats</ORDER>Fermat<APOS/>s Little Theorem</INDEX>
      If <LATEXINLINE>$n$</LATEXINLINE> is a prime number and 
      <LATEXINLINE>$a$</LATEXINLINE> is any positive integer less than 
      <LATEXINLINE>$n$</LATEXINLINE>, then <LATEXINLINE>$a$</LATEXINLINE> raised
      to the <LATEXINLINE>$n$</LATEXINLINE>th power is congruent to
      <LATEXINLINE>$a$</LATEXINLINE> modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE>.
    </BLOCKQUOTE>
    (Two numbers are said to be
    <INDEX>congruent modulo <LATEXINLINE>$n$</LATEXINLINE></INDEX>
    <EM>congruent modulo</EM>
    <LATEXINLINE>$n$</LATEXINLINE> if they both have the same remainder when
    divided by <LATEXINLINE>$n$</LATEXINLINE>.  The remainder of a number
    <LATEXINLINE>$a$</LATEXINLINE> when divided by 
    <LATEXINLINE>$n$</LATEXINLINE> is also referred to as the 
    <INDEX>remainder<SUBINDEX>modulo <LATEXINLINE>$n$</LATEXINLINE></SUBINDEX></INDEX>
    <INDEX>modulo <LATEXINLINE>$n$</LATEXINLINE></INDEX>
    <EM>remainder of</EM> <LATEXINLINE>$a$</LATEXINLINE> <EM>modulo</EM>
    <LATEXINLINE>$n$</LATEXINLINE>, or simply as <LATEXINLINE>$a$</LATEXINLINE> 
    <EM>modulo</EM> <LATEXINLINE>$n$</LATEXINLINE>.)
  </TEXT>

  <TEXT>
    If <LATEXINLINE>$n$</LATEXINLINE> is not prime, then, in general, most of
    the numbers <LATEXINLINE>$a &lt; n$</LATEXINLINE> will not satisfy the above
    relation.  This leads to the following algorithm for testing primality:
    Given a number <LATEXINLINE>$n$</LATEXINLINE>, pick a 
    <INDEX>random-number generator<SUBINDEX><ORDER>primality</ORDER>in primality testing</SUBINDEX></INDEX>
    random number <LATEXINLINE>$a &lt; n$</LATEXINLINE> and compute the
    remainder of <LATEXINLINE>$a^n$</LATEXINLINE> modulo
    <LATEXINLINE>$n$</LATEXINLINE>.  If the result is not equal to
    <LATEXINLINE>$a$</LATEXINLINE>, then <LATEXINLINE>$n$</LATEXINLINE> is
    certainly not prime.  If it is <LATEXINLINE>$a$</LATEXINLINE>, then chances
    are good that <LATEXINLINE>$n$</LATEXINLINE> is prime.  Now pick another
    random number <LATEXINLINE>$a$</LATEXINLINE> and test it with the same
    method.  If it also satisfies the equation, then we can be even more
    confident that <LATEXINLINE>$n$</LATEXINLINE> is prime.  By trying more and
    more values of <LATEXINLINE>$a$</LATEXINLINE>, we can increase our
    confidence in the result.  This algorithm is known as the Fermat test.
  </TEXT>

  <TEXT>
    To implement the Fermat test, we need a
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE>
    that computes the
    <INDEX>exponentiation<SUBINDEX>modulo <LATEXINLINE>$n$</LATEXINLINE></SUBINDEX></INDEX>
    exponential of a number modulo another number:
    <SNIPPET PAGE="51">
      <INDEX><DECLARATION>expmod</DECLARATION><FRAGILE/></INDEX>
      <NAME>expmod_definition</NAME>
      <EXAMPLE>expmod_example</EXAMPLE>
      <REQUIRES>even_definition</REQUIRES>
      <REQUIRES>square_definition</REQUIRES>
      <SCHEME>
(define (expmod base exp m)
  (cond ((= exp 0) 1)
        ((even? exp)
         (remainder 
           (square (expmod base (/ exp 2) m))
           m))
        (else
         (remainder 
           (* base (expmod base (- exp 1) m))
           m))))        
      </SCHEME>
      <PYTHON>
def expmod(base, exp, m):
    return (1 if exp == 0
            else square(expmod(base, exp // 2, m)) % m if is_even(exp)
            else (base * expmod(base, exp - 1, m)) % m)
      </PYTHON>
    </SNIPPET>

    <SNIPPET PAGE="51" HIDE="yes">
      <NAME>expmod_example</NAME>
      <REQUIRES>expmod_definition</REQUIRES>
      <EXPECTED>4</EXPECTED>
      <SCHEME>
(expmod 4 3 5)
      </SCHEME>
      <PYTHON>
print(expmod(4, 3, 5))
      </PYTHON>
    </SNIPPET>
    This is very similar to the 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>fast-expt</SCHEMEINLINE></SCHEME>
      <PYTHON><PYTHONINLINE>fast_expt</PYTHONINLINE></PYTHON>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE>
    of section<SPACE/><REF NAME="sec:exponentiation"/>.  It uses successive
    squaring, so that the number of steps grows logarithmically with the
    exponent.<FOOTNOTE>The reduction steps in the cases where the exponent
    <LATEXINLINE>$e$</LATEXINLINE> is greater than 1 are based on the fact that,
    for any integers <LATEXINLINE>$x$</LATEXINLINE>,
    <LATEXINLINE>$y$</LATEXINLINE>, and <LATEXINLINE>$m$</LATEXINLINE>, we can
    find the remainder of <LATEXINLINE>$x$</LATEXINLINE> times
    <LATEXINLINE>$y$</LATEXINLINE> modulo <LATEXINLINE>$m$</LATEXINLINE> by
    computing separately the remainders of <LATEXINLINE>$x$</LATEXINLINE> modulo
    <LATEXINLINE>$m$</LATEXINLINE> and <LATEXINLINE>$y$</LATEXINLINE> modulo
    <LATEXINLINE>$m$</LATEXINLINE>, multiplying these, and then taking the
    remainder of the result modulo <LATEXINLINE>$m$</LATEXINLINE>.  For
    instance, in the case where <LATEXINLINE>$e$</LATEXINLINE> is even, we
    compute the remainder of <LATEXINLINE>$b^{e/2}$</LATEXINLINE> modulo
    <LATEXINLINE>$m$</LATEXINLINE>, square this, and take the remainder modulo
    <LATEXINLINE>$m$</LATEXINLINE>.  This technique is useful because it means
    we can perform our computation without ever having to deal with numbers much
    larger than <LATEXINLINE>$m$</LATEXINLINE>.  (Compare
    exercise<SPACE/><REF NAME="ex:Alyssas-expmod"/>.)</FOOTNOTE>
  </TEXT>

  <TEXT>
    The Fermat test is performed by choosing at random a number
    <LATEXINLINE>$a$</LATEXINLINE>  between 1 and
    <LATEXINLINE>$n-1$</LATEXINLINE> inclusive and checking whether the remainder
    modulo <LATEXINLINE>$n$</LATEXINLINE> of the
    <LATEXINLINE>$n$</LATEXINLINE>th power of <LATEXINLINE>$a$</LATEXINLINE> is
    equal to <LATEXINLINE>$a$</LATEXINLINE>.  The random number
    <LATEXINLINE>$a$</LATEXINLINE> is chosen using the
    <SPLITINLINE>
      <SCHEME>
	procedure
	<SCHEMEINLINE>random</SCHEMEINLINE>, 
      </SCHEME>
      <PYTHON>
	primitive function
	<PYTHONINLINE>random_random</PYTHONINLINE>, 
      </PYTHON>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>which we assume is included as a primitive in Scheme.</SCHEME>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>
	<INDEX><USE>random</USE> (primitive function)</INDEX>
	<INDEX><PRIMITIVE/><SUBINDEX><USE>random</USE></SUBINDEX></INDEX>
        <SCHEMEINLINE>Random</SCHEMEINLINE> 
	returns a nonnegative integer less than its integer input.  Hence, to obtain
	a random number between 1 and <LATEXINLINE>$n-1$</LATEXINLINE>, we call
	<SCHEMEINLINE>random</SCHEMEINLINE> with an input of
	<LATEXINLINE>$n-1$</LATEXINLINE> and add 1 to the result:	
      </SCHEME>
      <PYTHON>
	<INDEX><USE>random_random</USE> (primitive function)</INDEX>
	<INDEX><PRIMITIVE/><SUBINDEX><USE>random_random</USE><ECMA>Math.random</ECMA></SUBINDEX></INDEX>
	which returns a nonnegative number less than 1.  Hence, to obtain
	a random number between 1 and <LATEXINLINE>$n-1$</LATEXINLINE>, we multiply
	the return value of <PYTHONINLINE>random_random</PYTHONINLINE> by
	<LATEXINLINE>$n-1$</LATEXINLINE>, round down the result with the
	primitive function
	<INDEX><USE>math_floor</USE> (primitive function)</INDEX>
	<INDEX><PRIMITIVE/><SUBINDEX><USE>math_floor</USE><ECMA>Math.floor</ECMA></SUBINDEX></INDEX>
	<PYTHONINLINE>math_floor</PYTHONINLINE>,
	and add 1:
      </PYTHON>
    </SPLITINLINE>
    <SNIPPET PAGE="52" HIDE="yes">
      <NAME>random_definition</NAME>
      <SCHEME>
;; random is predefined in Scheme
      </SCHEME>
      <PYTHON>
def random(n):
    return math_floor(random_random() * n)
      </PYTHON>
    </SNIPPET>
    <SNIPPET PAGE="52">
      <INDEX><DECLARATION>fermat_test</DECLARATION></INDEX>
      <NAME>fermat_test_definition</NAME>
      <REQUIRES>square_definition</REQUIRES>
      <REQUIRES>expmod_definition</REQUIRES>
      <REQUIRES>random_definition</REQUIRES>
      <EXAMPLE>fermat_test_example</EXAMPLE>
      <SCHEME>
(define (fermat-test n)
  (define (try-it a)
    (= (expmod a n n) a))
  (try-it (+ 1 (random (- n 1)))))
      </SCHEME>
      <PYTHON>
def fermat_test(n):
    def try_it(a):
        return expmod(a, n, n) == a
    return try_it(1 + math_floor(random_random() * (n - 1)))
      </PYTHON>
    </SNIPPET>

    <SNIPPET PAGE="51" HIDE="yes">
      <NAME>fermat_test_example</NAME>
      <REQUIRES>fermat_test_definition</REQUIRES>
      <EXPECTED>true</EXPECTED>
      <SCHEME>
(fermat-test 97)
      </SCHEME>
      <PYTHON>
print(fermat_test(97))
      </PYTHON>
    </SNIPPET>
  </TEXT>

  <TEXT>
    The following 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    runs the test a given number of times, as specified by a parameter.  Its
    value is true if the test succeeds every time, and false otherwise.
    <SNIPPET PAGE="52" POSTPADDING="no">
      <INDEX><DECLARATION>fast_is_prime</DECLARATION></INDEX>
      <NAME>fast_prime_definition</NAME>
      <REQUIRES>square_definition</REQUIRES>
      <REQUIRES>expmod_definition</REQUIRES>
      <REQUIRES>random_definition</REQUIRES>
      <REQUIRES>fermat_test_definition</REQUIRES>
      <EXAMPLE>fast_prime_example</EXAMPLE>
      <SCHEME>
(define (fast-prime? n times)
  (cond ((= times 0) true)
        ((fermat-test n) 
         (fast-prime? n (- times 1)))
        (else false)))
      </SCHEME>
      <PYTHON>
def fast_is_prime(n, times):
    return (True if times == 0
            else fast_is_prime(n, times - 1) if fermat_test(n)
            else False)
      </PYTHON>
    </SNIPPET>
    <SNIPPET PAGE="52" HIDE="yes">
      <NAME>fast_prime_example</NAME>
      <REQUIRES>fast_prime_definition</REQUIRES>
      <EXPECTED>true</EXPECTED>
      <SCHEME>
(fast-prime? 97 3)
      </SCHEME>
      <PYTHON>
print(fast_is_prime(97, 3))
      </PYTHON>
    </SNIPPET>
  </TEXT>

  <SUBHEADING>
    <NAME>Probabilistic methods</NAME>
  </SUBHEADING>

  <INDEX>probabilistic algorithm<OPEN/></INDEX>
  <INDEX>algorithm<SUBINDEX>probabilistic<OPEN/></SUBINDEX></INDEX>

  <TEXT>
    The Fermat test differs in character from most familiar algorithms, in which
    one computes an answer that is guaranteed to be correct.  Here, the answer
    obtained is only probably correct.  More precisely, if
    <LATEXINLINE>$n$</LATEXINLINE> ever fails the Fermat test, we can be certain
    that <LATEXINLINE>$n$</LATEXINLINE> is not prime. But the fact that
    <LATEXINLINE>$n$</LATEXINLINE> passes the test, while an extremely strong
    indication, is still not a guarantee that <LATEXINLINE>$n$</LATEXINLINE> is
    prime.  What we would like to say is that for any number
    <LATEXINLINE>$n$</LATEXINLINE>, if we perform the test enough times and find
    that <LATEXINLINE>$n$</LATEXINLINE> always passes the test, then the
    probability of error in our primality test can be made as small as we like.
  </TEXT>

  <TEXT>
    Unfortunately, this assertion is not quite correct.  There do exist numbers
    that fool the Fermat test: numbers <LATEXINLINE>$n$</LATEXINLINE> that are
    not prime and yet have the property that <LATEXINLINE>$a^n$</LATEXINLINE> is
    congruent to <LATEXINLINE>$a$</LATEXINLINE> modulo
    <LATEXINLINE>$n$</LATEXINLINE> for all integers
    <LATEXINLINE>$a &lt; n$</LATEXINLINE>.  Such numbers are extremely rare, so
    the Fermat test is quite reliable in practice.<FOOTNOTE>
    <LABEL NAME="foot:carmichaelfn"/>
    Numbers that fool the
    Fermat test are called
    <INDEX>Carmichael numbers</INDEX>
    <EM>Carmichael numbers</EM>, and little is known
    about them other than that they are extremely rare.  There are 255
    Carmichael numbers below 100,000,000.  The smallest few are 561, 1105,
    1729, 2465, 2821, and 6601.  In testing primality of very large
    numbers chosen at random, the chance of stumbling upon a value that
    fools the Fermat test is less than the chance that 
    <INDEX>cosmic radiation</INDEX>
    cosmic radiation will cause the computer to make an error in carrying out a
    <QUOTE>correct</QUOTE> algorithm.  Considering an algorithm to be inadequate
    for the first reason but not for the second illustrates the difference
    between
    <INDEX>engineering vs.<SPACE/>mathematics</INDEX>
    <INDEX>mathematics<SUBINDEX>engineering vs.</SUBINDEX></INDEX>
    mathematics and engineering.
  </FOOTNOTE>
    There are variations of the Fermat test that cannot be fooled.  In these
    tests, as with the Fermat method, one tests the primality of an integer
    <LATEXINLINE>$n$</LATEXINLINE> by choosing a random integer
    <LATEXINLINE>$a &lt; n$</LATEXINLINE> and checking some condition that
    depends upon <LATEXINLINE>$n$</LATEXINLINE> and
    <LATEXINLINE>$a$</LATEXINLINE>.  (See
    exercise<SPACE/><REF NAME="ex:miller-rabin"/> for an example of such a test.)
    On the other hand, in contrast to the Fermat test, one can prove that, for
    any <LATEXINLINE>$n$</LATEXINLINE>, the condition does not hold for most of
    the integers <LATEXINLINE>$a &lt; n$</LATEXINLINE> unless
    <LATEXINLINE>$n$</LATEXINLINE> is prime.  Thus, if
    <LATEXINLINE>$n$</LATEXINLINE> passes the test for some random choice
    of<SPACE/><LATEXINLINE>$a$</LATEXINLINE>, the chances are better than even
    that <LATEXINLINE>$n$</LATEXINLINE> is prime.  If
    <LATEXINLINE>$n$</LATEXINLINE> passes the test for two random choices of
    <LATEXINLINE>$a$</LATEXINLINE>, the chances are better than 3 out of 4 that
    <LATEXINLINE>$n$</LATEXINLINE> is prime. By running the test with more and
    more randomly chosen values of <LATEXINLINE>$a$</LATEXINLINE> we can make
    the probability of error as small as we like.
    <INDEX>Fermat test for primality<CLOSE/></INDEX>
    <INDEX>prime number(s)<SUBINDEX>Fermat test for<CLOSE/></SUBINDEX></INDEX>
  </TEXT>

  <TEXT>
    The existence of tests for which one can prove that the chance of error
    becomes arbitrarily small has sparked interest in algorithms of this type,
    which have come to be known as <EM>probabilistic algorithms</EM>.  There is
<INDEX>probabilistic algorithm<CLOSE/></INDEX>
<INDEX>algorithm<SUBINDEX>probabilistic<CLOSE/></SUBINDEX></INDEX>
<INDEX>prime number(s)<CLOSE/></INDEX>
    a great deal of research activity in this area, and probabilistic algorithms
    have been fruitfully applied to many fields.<FOOTNOTE>One of the most
    striking applications of
    probabilistic prime testing has been to the field of
    <INDEX>cryptography</INDEX>
    cryptography.
    <SPLITINLINE>
      <SCHEME>
	Although it is now computationally infeasible to factor an arbitrary 200-digit
	number, the primality of such a number can be checked in a few seconds with the
	Fermat test.
      </SCHEME>
      <PYTHON>
	Although it is computationally infeasible to factor an arbitrary 300-digit
	number as of this writing (2021), the primality of such a number can be checked
	in a few seconds with the Fermat test.
      </PYTHON>
    </SPLITINLINE>
    This fact forms the basis of a technique for constructing
    <QUOTE>unbreakable codes</QUOTE> suggested by 
    <INDEX>Rivest, Ronald L.</INDEX>
    Rivest,
    <INDEX>Shamir, Adi</INDEX>
    Shamir, and 
    <INDEX>Adleman, Leonard</INDEX>
    Adleman (1977).  The resulting 
    <INDEX>RSA algorithm</INDEX>
    <EM>RSA algorithm</EM> has become a widely used technique for enhancing the
    security of electronic communications.  Because of this and related
    developments, the study of 
    <INDEX>prime number(s)<SUBINDEX>cryptography and</SUBINDEX></INDEX>
    prime numbers, once considered the epitome of a topic in <QUOTE>pure</QUOTE>
    mathematics to be studied only for its own sake, now turns out to have
    important practical applications to cryptography, electronic funds transfer,
    and information retrieval.</FOOTNOTE>
  </TEXT>

  <EXERCISE>
    <LABEL NAME="ex:use-smallest-divisor"/> 
    Use the 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>smallest-divisor</SCHEMEINLINE></SCHEME>
      <PYTHON><PYTHONINLINE>smallest_divisor</PYTHONINLINE>
      </PYTHON>
    </SPLITINLINE> 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    to find the smallest divisor of each of the following numbers: 199, 1999,
    19999.
    <SOLUTION>
      <SNIPPET>
	<NAME>smallest_division_solution</NAME>
	<EXPECTED>199</EXPECTED>
	<REQUIRES>smallest_divisor_definition</REQUIRES>
	<PYTHON>
print(smallest_divisor(199))
# print(smallest_divisor(1999))
# print(smallest_divisor(19999))
        </PYTHON>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
    </SOLUTION>
  </EXERCISE>

  <EXERCISE>
    <LABEL NAME="ex:search-for-primes"/>
    <SPLITINLINE>
      <SCHEME>
        Most Lisp implementations include a primitive called 
        <INDEX><DECLARATION>runtime</DECLARATION></INDEX> 
        <INDEX><DECLARATION>primitive <SPLITINLINE><SCHEME>procedures</SCHEME><PYTHON>functions</PYTHON></SPLITINLINE> (those marked <EM>ns</EM> are not in the IEEE Scheme standard)</DECLARATION><SUBINDEX><ORDER>runtime</ORDER><SCHEMEINLINE>runtime</SCHEMEINLINE> (<EM>ns</EM>)</SUBINDEX></INDEX>
        <SCHEMEINLINE>runtime</SCHEMEINLINE>
        that returns an integer that specifies the amount of time the system has
	been running (measured, for example, in microseconds).  The following 
        <SCHEMEINLINE>timed-prime-test</SCHEMEINLINE>
	procedure,
      </SCHEME>
      <PYTHON>
        Assume a primitive function
	<INDEX><USE>get_time</USE> (primitive function)</INDEX>
	<INDEX><PRIMITIVE/><SUBINDEX><USE>get_time</USE><ECMA>new Date().getTime</ECMA></SUBINDEX></INDEX>
	<PYTHONINLINE>get_time</PYTHONINLINE> of no arguments
	that returns the number of milliseconds that have passed since 00:00:00 UTC
	on Thursday, 1 January, 1970.<FOOTNOTE>This date is called the
	<INDEX>UNIX<SUBINDEX>epoch</SUBINDEX></INDEX>
	<EM>UNIX epoch</EM> and is part of the specification of functions
	that deal with time in the
	UNIX<LATEXINLINE>$^{\textrm{TM}}$</LATEXINLINE> operating
	system.</FOOTNOTE>
	The following 
        <PYTHONINLINE>timed_prime_test</PYTHONINLINE>
	function,
      </PYTHON>
    </SPLITINLINE>
    when called with an integer <LATEXINLINE>$n$</LATEXINLINE>, prints
    <LATEXINLINE>$n$</LATEXINLINE> and checks to see if
    <LATEXINLINE>$n$</LATEXINLINE> is prime.  If <LATEXINLINE>$n$</LATEXINLINE>
    is prime, the 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE>
    prints three asterisks<SPLITINLINE><PYTHON><FOOTNOTE>The primitive
    function <PYTHONINLINE>print</PYTHONINLINE> prints its
    argument. Here
    <PYTHONINLINE>"</PYTHONINLINE><FIXED_SPACE/><PYTHONINLINE>***</PYTHONINLINE><FIXED_SPACE/><PYTHONINLINE>"</PYTHONINLINE>
    is a
    <EM>string</EM>, a sequence of characters that we pass as argument
    to the <PYTHONINLINE>print</PYTHONINLINE> function.
    Section<SPACE/><REF NAME="sec:strings"/> introduces strings more
    thoroughly.</FOOTNOTE></PYTHON></SPLITINLINE> followed by the amount of
    time used in performing the test.
    <SPLITINLINE>
      <SCHEME>
	<INDEX><DECLARATION>newline</DECLARATION></INDEX>
      </SCHEME>
    </SPLITINLINE>
    <SNIPPET PAGE="52">
      <INDEX><USE>print</USE> (primitive function)<FRAGILE/></INDEX>
      <INDEX><USE>timed_prime_test</USE><FRAGILE/></INDEX>
      <NAME>timed_prime_definition</NAME>
      <REQUIRES>prime_definition</REQUIRES>
      <EXAMPLE>timed_prime_example</EXAMPLE>
      <SCHEME>
(define (timed-prime-test n)
  (newline)
  (display n)
  (start-prime-test n (runtime)))

(define (start-prime-test n start-time)
  (if (prime? n)
      (report-prime (- (runtime) start-time))))

(define (report-prime elapsed-time)
  (display " *** ")
  (display elapsed-time))
      </SCHEME>
      <PYTHON>
def timed_prime_test(n):
    print(n)
    return start_prime_test(n, get_time())
<SHORT_SPACE_AND_ALLOW_BREAK/>
def start_prime_test(n, start_time):
    return (report_prime(get_time() - start_time) if is_prime(n)
            else False)
<SHORT_SPACE_AND_ALLOW_BREAK/>
def report_prime(elapsed_time):
    print(" *** ")
    print(elapsed_time)
    return True
      </PYTHON>
    </SNIPPET>
    <SNIPPET PAGE="52" HIDE="yes">
      <NAME>timed_prime_example</NAME>
      <REQUIRES>timed_prime_definition</REQUIRES>
      <SCHEME>
(timed-prime-test 43)
      </SCHEME>
      <PYTHON>
timed_prime_test(43)
      </PYTHON>
    </SNIPPET>
    Using this 
    <SPLITINLINE>
      <SCHEME>procedure,</SCHEME>
      <PYTHON>function,</PYTHON>
    </SPLITINLINE>
    write a 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>search-for-primes</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>search_for_primes</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    that checks the primality of consecutive odd integers in a specified range.
    Use your 
    <SPLITINLINE>
      <SCHEME>
        procedure
      </SCHEME>
      <PYTHON>
        function
      </PYTHON>
    </SPLITINLINE>
    to find the three smallest primes larger than 1000; larger than 10,000;
    larger than 100,000; larger than 1,000,000.  Note the time needed to test
    each prime.  Since the testing algorithm has order of growth of
    <LATEXINLINE>$\Theta(\sqrt{n})$</LATEXINLINE>, you should expect that testing
    for primes around 10,000 should take about
    <LATEXINLINE>$\sqrt{10}$</LATEXINLINE> times as long as testing for primes
    around 1000.  Do your timing data bear this out? How well do the data for
    100,000 and 1,000,000 support the <LATEXINLINE>$\sqrt{n}$</LATEXINLINE>
    prediction?  Is your result compatible with the notion that programs on
    your machine run in time proportional to the number of steps required for
    the computation?
    <SOLUTION>
      <SNIPPET>
        <NAME>search_for_primes_definition</NAME>
        <EXAMPLE>search_for_primes_example</EXAMPLE>
        <REQUIRES>timed_prime_definition</REQUIRES>
        <PYTHON>
def search_for_primes(start, times):
    return (
        True if times == 0
        else search_for_primes(start + 1, times)
             if start &gt; 2 and start % 2 == 0
        # if we get true, it's a prime
        else search_for_primes(start + 2, times - 1)
             if timed_prime_test(start)
        else search_for_primes(start + 2, times)
    )
        </PYTHON>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
      <SNIPPET HIDE="yes">
      <NAME>search_for_primes_example</NAME>
      <REQUIRES>search_for_primes_definition</REQUIRES>
      <PYTHON>
search_for_primes(10000, 3)
# search_for_primes(10000000, 3)
# search_for_primes(10000000000, 3)
      </PYTHON>
      <SCHEME>
      </SCHEME>
    </SNIPPET>
      The timing data quite clearly supports the <LATEXINLINE>$\sqrt{n}$</LATEXINLINE> for prediction that is sufficiently big, such as 100,000 and 1,000,000.
    </SOLUTION>
  </EXERCISE>

  <EXERCISE>
    <LABEL NAME="ex:better-smallest-divisor"/> 
    The 
    <INDEX><USE>smallest_divisor</USE><SUBINDEX>more efficient version</SUBINDEX></INDEX>
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>smallest-divisor</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>smallest_divisor</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    <SPLITINLINE><SCHEME>procedure</SCHEME><PYTHON>function</PYTHON></SPLITINLINE> 
    shown at the start of this section does lots of needless testing: After it
    checks to see if the number is divisible by 2 there is no point in checking
    to see if it is divisible by any larger even numbers.  This suggests that
    the values used for 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>test-divisor</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>test_divisor</PYTHONINLINE>
      </PYTHON>
    </SPLITINLINE>
    should not be 2, 3, 4, 5, 6, <ELLIPSIS/> but rather 2, 3, 5, 7, 9,
    <ELLIPSIS/>. To implement this change, 
    <SPLITINLINE>
      <SCHEME>define a procedure</SCHEME>
      <PYTHON>define a function</PYTHON>
    </SPLITINLINE> 
    <SPLITINLINE><SCHEME><SCHEMEINLINE>next</SCHEMEINLINE></SCHEME><PYTHON><PYTHONINLINE>next_int</PYTHONINLINE></PYTHON></SPLITINLINE> that returns 3 if its input is equal to 2
    and otherwise returns its input plus<SPACE/>2. Modify the 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>smallest-divisor</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>smallest_divisor</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    to use 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>(next test-divisor)</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>next_int(test_divisor)</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    instead of 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>(+ test-divisor 1)</SCHEMEINLINE>.</SCHEME>
      <PYTHON><PYTHONINLINE>test_divisor + 1</PYTHONINLINE>.
      </PYTHON></SPLITINLINE>
    With 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>timed-prime-test</SCHEMEINLINE>
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>timed_prime_test</PYTHONINLINE>
      </PYTHON>
    </SPLITINLINE>
    incorporating this modified version of 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>smallest-divisor</SCHEMEINLINE>,</SCHEME>
      <PYTHON><PYTHONINLINE>smallest_divisor</PYTHONINLINE>,
      </PYTHON>
      </SPLITINLINE>
    run the test for each of the 12 primes found in
    exercise<SPACE/><REF NAME="ex:search-for-primes"/>.  
    Since this modification halves the number of test steps, you should expect
    it to run about twice as fast. Is this expectation confirmed?  If not, what
    is the observed ratio of the speeds of the two algorithms, and how do you
    explain the fact that it is different from 2?
    <SOLUTION>
      <SNIPPET>
        <NAME>better_smallest_divisor_solution</NAME>
        <PYTHON>
def next_int(input):
    return 3 if input == 2 else input + 2

def find_divisor(n, test_divisor):
    return (n if square(test_divisor) &gt; n
            else test_divisor if divides(test_divisor, n)
            else find_divisor(n, next_int(test_divisor)))
        </PYTHON>
	<PYTHON_RUN>
def square(x):
    return x * x
def smallest_divisor(n):
    return find_divisor(n, 2)
def divides(a, b):
    return b % a == 0
def is_prime(n):
    return n == smallest_divisor(n)
def timed_prime_test(n):
    print(n)
    return start_prime_test(n, get_time())
def start_prime_test(n, start_time):
    return (report_prime(get_time() - start_time) if is_prime(n)
            else True)
def report_prime(elapsed_time):
    print(" *** ")
    print(elapsed_time)
def next_int(input):
    return 3 if input == 2 else input + 2
def find_divisor(n, test_divisor):
    return (n if square(test_divisor) &gt; n
            else test_divisor if divides(test_divisor, n)
            else find_divisor(n, next_int(test_divisor)))

timed_prime_test(43)
	</PYTHON_RUN>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
      The ratio of the speeds of the two algorithms is not exactly 2, but this might be due to hardware / network issues.
      It is about 1.5 times faster compared to previous solution.
    </SOLUTION>
  </EXERCISE>

  <EXERCISE> 
    Modify the 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>timed-prime-test</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>timed_prime_test</PYTHONINLINE>
      </PYTHON>
    </SPLITINLINE>
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    of exercise<SPACE/><REF NAME="ex:search-for-primes"/> to use 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>fast-prime?</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>fast_is_prime</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    (the Fermat method), and test each of the 12 primes you found in that
    exercise.  Since the Fermat test has
    <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE> growth, how would you expect
    the time to test primes near 1,000,000 to compare with the time needed to
    test primes near 1000?  Do your data bear this out?  Can you explain any
    discrepancy you find?
    <LABEL NAME="ex:mod-timed-prime-test"/>
        <SOLUTION>
      <SNIPPET>
      <EXAMPLE>mod_timed_prime_test_example</EXAMPLE>
	<NAME>mod_timed_prime_test_solution</NAME>
	<REQUIRES>fast_prime_definition</REQUIRES>
	<PYTHON>
def timed_prime_test(n):
    print(n)
    return start_prime_test(n, get_time())
def start_prime_test(n, start_time):
    return (report_prime(get_time() - start_time)
            if fast_is_prime(n, math_floor(math_log(n)))
            else True)
def report_prime(elapsed_time):
    print(" *** ")
    print(elapsed_time)
        </PYTHON>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
      <SNIPPET HIDE="yes">
      <NAME>mod_timed_prime_test_example</NAME>
      <REQUIRES>mod_timed_prime_test_solution</REQUIRES>
      <PYTHON>
timed_prime_test(10007)
# timed_prime_test(10009)
# timed_prime_test(10037)
# timed_prime_test(100003, 3)
# timed_prime_test(100019, 3)
# timed_prime_test(100043, 3)
# timed_prime_test(1000003, 3)
# timed_prime_test(1000033, 3)
# timed_prime_test(1000037, 3)
      </PYTHON>
      <SCHEME>
      </SCHEME>
    </SNIPPET>
        The time to test primes near 1,000,000 using <PYTHONINLINE>fast_is_prime</PYTHONINLINE> 
        is about 4 ms, 4 times the time needed to test primes near 1,000. This is faster compared to 8 ms 
        that we achieved if we use the <PYTHONINLINE>is_prime</PYTHONINLINE>. However, despite being 4 times slower,
        this fact cannot lead us to believe that it has a greater growth than <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE>, 
        as it should be tested with greater numbers to gain a more accurate understanding of the growth of the function.
    </SOLUTION>
  </EXERCISE>

  <EXERCISE> 
    Alyssa P. Hacker complains that we went to a lot of extra work in writing
    <SCHEMEINLINE>expmod</SCHEMEINLINE>. After all, she says, since we already
    know how to compute exponentials, we could have simply written
    <SNIPPET PAGE="55">
      <INDEX><DECLARATION>expmod</DECLARATION><FRAGILE/></INDEX>
      <NAME>no_extra_work</NAME>
      <REQUIRES>expt_log_definition</REQUIRES>
      <EXAMPLE>expmod_example</EXAMPLE>
      <SCHEME>
(define (expmod base exp m)
  (remainder (fast-expt base exp) m))
      </SCHEME>
      <PYTHON>
def expmod(base, exp, m):
    return fast_expt(base, exp) % m
      </PYTHON>
    </SNIPPET>
    Is she correct?  
    Would this 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    serve as well for our fast prime tester?  Explain.
    <LABEL NAME="ex:Alyssas-expmod"/>
    <SOLUTION>
      Alyssa's suggestion is correct at first sight: her
      <PYTHONINLINE>expmod</PYTHONINLINE> function computes
      <LATEXINLINE>$\textit{base}^{\textit{exp}}$</LATEXINLINE> and
      then finds its remainder modulo <LATEXINLINE>$m$</LATEXINLINE>, as
      required in the Fermat test. Because Python integers have
      arbitrary precision, this even produces the exact answer, no
      matter how large the numbers involved become.
      <P/>
      It would not, however, serve as well for our fast prime tester.
      The problem is that <PYTHONINLINE>fast_expt(base, exp)</PYTHONINLINE>
      computes the full value of
      <LATEXINLINE>$\textit{base}^{\textit{exp}}$</LATEXINLINE> before
      any remainder is taken. When we test the primality of a number
      <LATEXINLINE>$n$</LATEXINLINE>, the exponent is
      <LATEXINLINE>$n$</LATEXINLINE> itself, so this intermediate value is
      astronomically large<EMDASH/>for instance,
      <PYTHONINLINE>fast_expt(2, 100)</PYTHONINLINE> is the 31-digit number
      1267650600228229401496703205376, and the values grow far larger
      still for the candidates of interest, which have hundreds or
      thousands of digits. Arithmetic on such large integers is slow: the
      time to multiply two integers grows with the number of digits they
      contain, so each of the multiplications carried out by
      <PYTHONINLINE>fast_expt</PYTHONINLINE> becomes progressively more
      expensive. This defeats the
      <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE> order of growth that
      made the fast prime tester attractive.
      <P/>
      The original <PYTHONINLINE>expmod</PYTHONINLINE> avoids the problem
      by taking the remainder modulo <LATEXINLINE>$m$</LATEXINLINE> at each
      step, so that the intermediate results are never larger than
      <LATEXINLINE>$m^2$</LATEXINLINE>. Every multiplication therefore
      involves only numbers smaller than <LATEXINLINE>$m^2$</LATEXINLINE>,
      and the computation stays fast.
      <P/>
      For small bases, however, Alyssa's method may be even faster than
      the original <PYTHONINLINE>expmod</PYTHONINLINE> function,
      because it will carry out only one single remainder operation.
    </SOLUTION>
  </EXERCISE>

  <EXERCISE>
    <LABEL NAME="ex:louis-fast-prime"/>
    Louis Reasoner is having great difficulty doing
    exercise<SPACE/><REF NAME="ex:mod-timed-prime-test"/>.
    His 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>fast-prime?</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>fast_is_prime</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    test seems to run more slowly than his 
    <SPLITINLINE>
      <SCHEME>
        <SCHEMEINLINE>prime?</SCHEMEINLINE> 
      </SCHEME>
      <PYTHON>
        <PYTHONINLINE>is_prime</PYTHONINLINE> 
      </PYTHON>
    </SPLITINLINE>
    test. Louis calls his friend Eva Lu Ator over to help.  When they examine
    Louis<APOS/>s code, they find that he has rewritten the 
    <SCHEMEINLINE>expmod</SCHEMEINLINE> 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE>
    to use an explicit multiplication, rather than calling
    <SCHEMEINLINE>square</SCHEMEINLINE>:
    <SNIPPET PAGE="55">
      <INDEX><DECLARATION>expmod</DECLARATION><FRAGILE/></INDEX>
      <REQUIRES>even_definition</REQUIRES>
      <EXAMPLE>expmod_example</EXAMPLE>
      <SCHEME>
(define (expmod base exp m)
  (cond ((= exp 0) 1)
        ((even? exp)
         (remainder 
           (* (expmod base (/ exp 2) m)
              (expmod base (/ exp 2) m))
           m))
        (else
         (remainder 
           (* base (expmod base (- exp 1) m))
           m))))
      </SCHEME>
      <PYTHON>
def expmod(base, exp, m):
    return (1 if exp == 0
            else (expmod(base, exp // 2, m)
                  * expmod(base, exp // 2, m)) % m if is_even(exp)
            else (base * expmod(base, exp - 1, m)) % m)
      </PYTHON>
    </SNIPPET>
    <QUOTE>I don<APOS/>t see what difference that could make,</QUOTE>
    says Louis. <QUOTE>I do.</QUOTE>  says Eva. <QUOTE>By writing the 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    like that, you have transformed the
    <LATEXINLINE>$\Theta(\log n)$</LATEXINLINE> process into a
    <LATEXINLINE>$\Theta(n)$</LATEXINLINE> process.</QUOTE> Explain.

    <SOLUTION>
      Eva is correct: by evaluating the expression:
      <SNIPPET EVAL="no">
	<PYTHON>
(expmod(base, exp // 2, m) * expmod(base, exp // 2, m)) % m    
	</PYTHON>
      </SNIPPET>
      the expression
      <PYTHONINLINE>expmod(base, exp // 2, m)</PYTHONINLINE>
      is evaluated twice at each step in the computation when the exponent is
      even, eliminating the benefit of the fast exponentiation
      algorithm<EMDASH/>which halves the exponent when the exponent is
      even<EMDASH/>therefore eliminating the feature of the algorithm that makes
      it faster. 
    </SOLUTION>
  </EXERCISE>

  <EXERCISE>
    Demonstrate that the
    <INDEX>Carmichael numbers</INDEX>
    Carmichael numbers listed in
    footnote<SPACE/><REF NAME="foot:carmichaelfn"/> really do fool the Fermat
    test.  That is, write a 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    that takes an integer <LATEXINLINE>$n$</LATEXINLINE> and tests whether
    <LATEXINLINE>$a^n$</LATEXINLINE> is congruent to 
    <LATEXINLINE>$a$</LATEXINLINE> modulo <LATEXINLINE>$n$</LATEXINLINE> for
    every <LATEXINLINE>$a &lt; n$</LATEXINLINE>, and try your 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    on the given Carmichael numbers.
    <SOLUTION>
      <SNIPPET>
	<NAME>carmichael</NAME>
        <EXAMPLE>example_1.29</EXAMPLE>
        <REQUIRES>even_definition</REQUIRES>
        <REQUIRES>square_definition</REQUIRES>
        <PYTHON>
def carmichael(n):
    def expmod(base, exp, m):
        return (1 if exp == 0
                else square(expmod(base, exp // 2, m)) % m if is_even(exp)
                else (base * expmod(base, exp - 1, m)) % m)
    def fermat_test(n, a):
        return expmod(a, n, n) == a
    def iterate(n, i):
        return (True if i == n
                else iterate(n, i + 1) if fermat_test(n, i)
                else False)
    return iterate(n, 2)
	</PYTHON>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
    </SOLUTION>
    <SNIPPET HIDE="yes">
      <NAME>example_1.29</NAME>
      <REQUIRES>carmichael</REQUIRES>
      <EXPECTED>true</EXPECTED>
      <PYTHON>
print(carmichael(561))
# carmichael(1105)
# carmichael(1729)
# carmichael(2465)
# carmichael(2821)
# carmichael(6601)
      </PYTHON>
      <SCHEME>
      </SCHEME>
    </SNIPPET>
    <LABEL NAME="ex:1_27"/>
  </EXERCISE>
        
  <EXERCISE>
    One variant of the Fermat test that cannot be fooled is called the
    <INDEX>prime number(s)<SUBINDEX>Miller<ENDASH/>Rabin test for</SUBINDEX></INDEX>
    <INDEX>Fermat test for primality<SUBINDEX>variant of</SUBINDEX></INDEX>
    <INDEX>Miller<ENDASH/>Rabin test for primality</INDEX>
    <INDEX>Miller, Gary L.</INDEX>
    <INDEX>Rabin, Michael O.</INDEX>
    <EM>Miller<ENDASH/>Rabin test</EM> (<CITATION>Miller 1976</CITATION>;
    <CITATION>Rabin 1980</CITATION>).  This starts from
    an alternate form of
    <INDEX><ORDER>Fermats</ORDER>Fermat<APOS/>s Little Theorem<SUBINDEX>alternate form</SUBINDEX></INDEX>
    Fermat<APOS/>s Little Theorem, which states that if
    <LATEXINLINE>$n$</LATEXINLINE> is a prime number and
    <LATEXINLINE>$a$</LATEXINLINE> is any positive integer less than
    <LATEXINLINE>$n$</LATEXINLINE>, then <LATEXINLINE>$a$</LATEXINLINE> raised
    to the <LATEXINLINE>$(n-1)$</LATEXINLINE>st power is congruent to 1
    modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE>.  To test the primality of a
    number <LATEXINLINE>$n$</LATEXINLINE> by the Miller<ENDASH/>Rabin test, we pick a
    random number <LATEXINLINE>$a &lt; n$</LATEXINLINE> and raise 
    <LATEXINLINE>$a$</LATEXINLINE> to the <LATEXINLINE>$(n-1)$</LATEXINLINE>st
    power modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE> using the
    <SCHEMEINLINE>expmod</SCHEMEINLINE> 
    <SPLITINLINE>
      <SCHEME>procedure.</SCHEME>
      <PYTHON>function.</PYTHON>
    </SPLITINLINE>
    However, whenever we perform the squaring step in
    <SCHEMEINLINE>expmod</SCHEMEINLINE>, we check to see if we have discovered a
    <QUOTE>nontrivial square root of 1
    modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE>,</QUOTE> 
    that is, a number not equal to 1 or <LATEXINLINE>$n-1$</LATEXINLINE> whose
    square is equal to 1 modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE>.  It is
    possible to prove that if such a nontrivial square root of 1 exists, then
    <LATEXINLINE>$n$</LATEXINLINE> is not prime. It is also possible to prove
    that if <LATEXINLINE>$n$</LATEXINLINE> is an odd number that is not prime,
    then, for at least half the numbers <LATEXINLINE>$a &lt; n$</LATEXINLINE>,
    computing <LATEXINLINE>$a^{n-1}$</LATEXINLINE> in this way will reveal a
    nontrivial square root of 1 modulo<SPACE/><LATEXINLINE>$n$</LATEXINLINE>.  
    (This is why the Miller<ENDASH/>Rabin test cannot be fooled.)  Modify the
    <SCHEMEINLINE>expmod</SCHEMEINLINE> 
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    to signal if it discovers a nontrivial square root of 1, and use this to
    implement the Miller<ENDASH/>Rabin test with a
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
      <PYTHON>function</PYTHON>
    </SPLITINLINE> 
    analogous to 
    <SPLITINLINE>
      <SCHEME><SCHEMEINLINE>fermat-test</SCHEMEINLINE>.</SCHEME>
      <PYTHON><PYTHONINLINE>fermat_test</PYTHONINLINE>.
    </PYTHON></SPLITINLINE>
    Check your
    <SPLITINLINE>
      <SCHEME>procedure</SCHEME>
    <PYTHON>function</PYTHON></SPLITINLINE> 
    by testing various known primes and non-primes. Hint: One convenient way to
    make <SCHEMEINLINE>expmod</SCHEMEINLINE> signal is to have it return<SPACE/>0.
    <LABEL NAME="ex:miller-rabin"/>
    <SOLUTION>
      <SNIPPET>
	<NAME>miller_rabin_test</NAME>
        <EXAMPLE>example_1.30</EXAMPLE>
        <REQUIRES>even_definition</REQUIRES>
        <REQUIRES>square_definition</REQUIRES>
        <PYTHON>
def random(n):
    return math_floor(random_random() * n)
def miller_rabin_test(n):
    def expmod(base, exp, m):
        return (1 if exp == 0
                else square(trivial_test(expmod(base, exp // 2, m), m)) % m if is_even(exp)
                else (base * expmod(base, exp - 1, m)) % m)
    def trivial_test(r, m):
        return (r if r == 1 or r == m - 1
                else 0 if square(r) % m == 1
                else r)
    def try_it(a):
        return expmod(a, n - 1, n) == 1
    return try_it(1 + random(n - 1))

def do_miller_rabin_test(n, times):
    return (True if times == 0
            else do_miller_rabin_test(n, times - 1) if miller_rabin_test(n)
            else False)
        </PYTHON>
        <SCHEME>
        </SCHEME>
      </SNIPPET>
    </SOLUTION>
    <SNIPPET HIDE="yes">
      <NAME>example_1.30</NAME>
      <REQUIRES>miller_rabin_test</REQUIRES>
      <EXPECTED>true</EXPECTED>
      <PYTHON>
print(do_miller_rabin_test(104743, 1000))
      </PYTHON>
      <SCHEME>
      </SCHEME>
    </SNIPPET>
  </EXERCISE>
  <INDEX>prime number(s)<SUBINDEX>testing for<CLOSE/></SUBINDEX></INDEX>
</SUBSECTION>
